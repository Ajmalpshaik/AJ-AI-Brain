# AJ Tools — Reusable AJ AI Bridge Scripts (index — start here)

This folder holds **working C# fragments**, not just descriptions of them, for jobs that come up
repeatedly on the live model via the AJ AI Bridge (`mcp__aj-tools-aj-ai__run_csharp`).
The point: the next session runs code that already worked, instead of re-deriving it from prose and
risking a small mistake creeping back in.

They're composed per request rather than rewritten each time. **Most requests split into "which elements"
(a `filters/` fragment — or `creators/` if they don't exist yet) + "what to do to them" (one or more
`actions/`).** Genuinely bespoke, order-dependent multi-stage builds live in `recipes/` instead.

**Read this file, pick the fragment, open that one file — don't read the whole folder.** Background lives
beside this index, not in it:

> **Faster: look it up instead of reading this.** `node tools/fragment-index.mjs --find <word>` searches
> every fragment's purpose and input fields and returns matches with their proven status;
> `--show <path>` prints one fragment's purpose plus exactly what you have to fill in. Same information
> as the tables below, computed from the fragments themselves, in one lookup instead of a 500-line read.
> **`--find` is one literal substring match, not a multi-word/AND search** — confirmed 2026-08-05:
> `--find "create sheet"` returned "Nothing matched" for a query typed as two natural words, even though
> `creators/create-sheet.cs` already existed with purpose text "Create one or more new sheets..." — the
> exact phrase "create sheet" is never adjacent in that sentence ("Create one or more **new** sheets"), so
> the substring check failed, while `--find "new sheet"` succeeded (matches inside "new sheet**s**"). A
> single-word query is safe; a multi-word query only hits if those words happen to sit adjacent, in that
> order, in the source text — it is not smart enough to find a fragment described in different words than
> your query. **Prefer single keywords, and if the first search comes back empty, retry with a different
> single word before concluding no fragment exists** — this exact miss led to writing a duplicate
> `create-sheets.cs` right next to the real, already-proven `create-sheet.cs` (caught by the
> consistency-hook's "script not in README" check, not by the search itself).
> It exists because this file being long is the reason "reuse before writing new C#" gets skipped —
> the fragment was never missing, just hard to find. Add `--verified` to see only what has actually run.

- **Why the folder is shaped this way** (the filter+action idea, a worked example, the local-AI workflow,
  how the library grows) → [`architecture.md`](architecture.md)

Everything below is what an actual script task needs: the routing table, the rules, and the checkpoints.

## Current fragments

### Filters (produce `elements`)
Grouped into subfolders under `filters/` by what you already know about the elements you want —
the same job-grouping idea used for `actions/`. Split from one flat 49-file folder on 2026-07-26.

**What the element IS — category, family, type, material, datum** — [`filters/by-identity/`](filters/by-identity/)
| Fragment | Job |
|---|---|
| [`filter-by-category.cs`](filters/by-identity/filter-by-category.cs) | Every instance of one category, optional level scope — ✓ verified 2026-08-06 (**fixed live: the level chain never tried `RBS_START_LEVEL_PARAM`, so setting `levelIdFilter` matched ZERO ducts and reported success** — 3 vs 0 side by side) |
| [`filter-by-category-and-family.cs`](filters/by-identity/filter-by-category-and-family.cs) | Category narrowed to a family name (VCD-style) — ✓ verified 2026-08-06, **both paths, and they cross-validate**: the LINQ name match and the `FamilyInstanceFilter` path each returned the same 5 elements by completely different mechanisms, so neither can be quietly wrong on its own. Proven zero on a bogus name |
| [`filter-by-category-name.cs`](filters/by-identity/filter-by-category-name.cs) | Category resolved by plain display name, not the BuiltInCategory enum — ✓ verified 2026-08-06 (name → Id -2008000 → 3 ducts, cross-checked against an independent `BuiltInCategory` baseline; a bogus name proved it takes the explicit "not found" message path rather than returning a silent 0) |
| [`filter-by-family.cs`](filters/by-identity/filter-by-family.cs) | Family name matched across the WHOLE model, no category picked first — ✓ verified 2026-08-06 (5 diffusers by family name, 1 door, case-insensitive, proven zero; an empty needle correctly matches all 10 FamilyInstances) |
| [`filter-by-family-type.cs`](filters/by-identity/filter-by-family-type.cs) | A specific Type inside a Family, matched by name (e.g. one exact fitting size) — ✓ verified 2026-08-06 (exact family+type → 5, with and without the category filter; right-family/wrong-type and wrong-family/right-type both proven zeros, so the AND is real; `string.Equals` with OrdinalIgnoreCase means the "exact" match is case-INsensitive — lowercase inputs still matched) |
| [`filter-by-grid.cs`](filters/by-identity/filter-by-grid.cs) | Every Grid, optional name substring — feeds `creators/create-dimension.cs` — ✓ verified 2026-08-06 (4 grids '1'-'4', name filter, ascending order asserted, proven zero) |
| [`filter-by-wrong-category.cs`](filters/by-identity/filter-by-wrong-category.cs) | Elements whose family/type name or Equipment Tag prefix says one thing but whose CATEGORY says another — "which of my louvres are not Air Terminals". Scans every category and keeps only the mismatches; reports OK vs WRONG per category so a loose keyword is visible before acting. Technique run live twice 2026-08-20 (BL006A 7 found/0 correct, BL003A 10 found/2 correct) and it drove a real re-categorisation — this generalised file is compile-checked, not yet run verbatim |
| [`filter-by-id-list.cs`](filters/by-identity/filter-by-id-list.cs) | A specific list of Element Ids the user already has — "what is this element / what are its parameters" — ✓ verified 2026-08-06 (4 real Ids + 1 bogus: reports found/missing correctly, does not throw on a dead Id; note `e.Name` on a duct returns its TYPE name) |
| [`filter-by-levels.cs`](filters/by-identity/filter-by-levels.cs) | Every Level ELEMENT itself (not elements sitting on one — that's `filter-by-elements-on-level.cs`), ordered by elevation — ✓ verified 2026-08-06 (case-insensitive match confirmed with a lowercase term, ascending order asserted, and a non-matching term proved a real 0) |
| [`filter-by-material.cs`](filters/by-identity/filter-by-material.cs) | Elements using a specific Revit Material, category-scoped — ✓ verified 2026-08-06 (the wall material was read off a real wall first, then matched: 8 walls, case-insensitive; wrong category is a proven zero; a bogus material name takes the explicit not-found path) |
| [`filter-by-multiple-categories.cs`](filters/by-identity/filter-by-multiple-categories.cs) | Several categories collected as one group, e.g. duct system / pipe system / cable tray system — ✓ verified 2026-08-06 (both scopes; 6 duct categories → 5 elements = 3 ducts + 2 fittings, with the 4 empty categories skipped silently and correctly. **Dedupe proven**: the same category listed three times returned 3, not 9) |
| [`filter-by-types.cs`](filters/by-identity/filter-by-types.cs) | The TYPE elements themselves (FamilySymbol or system-family type), matched by family/type name — reaches a type with zero placed instances, unlike the instance-derived type actions — ✓ verified 2026-08-06 (TYPES not instances: 9 air-terminal types, family and type-name filters combine correctly (7 → 2), and it also handles **system families** where there is no FamilySymbol — 6 WallTypes via `t.FamilyName`) |

**A parameter's VALUE — size, length, text, presence** — [`filters/by-property/`](filters/by-property/)
| Fragment | Job |
|---|---|
| [`filter-by-category-and-numeric-param.cs`](filters/by-property/filter-by-category-and-numeric-param.cs) | Category narrowed by a numeric parameter vs. an mm value (the "500mm duct" filter) — ✓ verified 2026-08-06, the most thorough in the library: **all four comparison modes** (eq/gte/lte/between) plus swapping `parameterName` to Width, an absent parameter (Diameter → skipped, no crash), a bogus name (no throw), and mm→ft (300mm = 0.984252 ft). Every count was predicted from the ducts' real 300mm size first, so each zero is a proven zero |
| [`filter-by-length.cs`](filters/by-property/filter-by-length.cs) | Category narrowed by Length (mm) vs. an mm value — bound to `CURVE_ELEM_LENGTH` directly — ✓ verified 2026-08-06 (**all four modes** on real lengths printed first — ducts 18234/4700/6150 mm and a 5000 mm pipe — so every count including three separate zeros is checkable. Also confirmed it works beyond MEP curves: 8 walls matched `gte 1`) |
| [`filter-by-parameter-exists.cs`](filters/by-property/filter-by-parameter-exists.cs) | Elements that have a given parameter attached, whether blank or not — QA sweep, distinct from `filter-by-parameter-text.cs`'s value match — ✓ verified 2026-08-06 (**all three modes**, and the difference between them is demonstrated rather than described: `Width` gives has 3 / hasvalue 3 / missing 0, while blank `Comments` gives has 3 / hasvalue 0 / missing 3. A non-existent parameter is a proven zero) |
| [`filter-by-parameter-text.cs`](filters/by-property/filter-by-parameter-text.cs) | Category or whole-model scan narrowed by text in family/type/parameter values — ✓ verified 2026-08-06 (**all six match modes** contains/notcontains/begins/ends/equals/notequals, plus the special `Family` / `Type` / `Family and Type` names and case-insensitivity. Both partitions balanced: contains+notcontains = 5 and equals+notequals = 5. The actual text of every element was printed first, so each zero is checkable — `ends 'Diffuser'` on **Type** is correctly 0, because the type name ends in "Connection") |
| [`filter-by-size.cs`](filters/by-property/filter-by-size.cs) | Category narrowed by size — round (Diameter) and rectangular (Width x Height) handled together, or a plain "Size" text match — ✓ verified 2026-08-06 (**both branches**: the Size-text path against the real `300x300` strings, and the numeric path for rectangular (W+H, and W alone) and round (pipe Diameter 50). Four proven zeros across the two branches) |

**WHERE it sits — room, space, region, real geometry intersection** — [`filters/by-location/`](filters/by-location/)
| Fragment | Job |
|---|---|
| [`filter-by-element-intersection.cs`](filters/by-location/filter-by-element-intersection.cs) | Elements whose real geometry intersects one specific target element (`ElementIntersectsElementFilter`) — ✓ verified 2026-08-06 **with a real positive**: 0 overlaps normally, then a duct was copied 100 mm sideways inside a transaction and each found the other (symmetric), while a second copy 50 m away was correctly NOT counted; rolled back to 0. Confirms the header's point — the door does NOT intersect its host wall, because hosting cuts an opening rather than overlapping |
| [`filter-by-elements-on-level.cs`](filters/by-location/filter-by-elements-on-level.cs) | Everything on a given Level across the WHOLE model, optional category scope — ✓ verified 2026-08-06 **end to end, and it is the fix**: Level 1 now returns 3 ducts and 1 pipe (both were **0** before the `RBS_START_LEVEL_PARAM` gap was closed), 8 walls via the never-broken Wall branch, and Level 2 returns 0 — a proven zero, since the same code gives 3 on Level 1 |
| [`filter-by-region.cs`](filters/by-location/filter-by-region.cs) | Category narrowed to instances whose bounding box intersects a given mm region — ✓ verified 2026-08-06 (duct bounding boxes printed first so the results are checkable: an enclosing box gives 3, a far-away box a proven zero, walls 8) |
| [`filter-by-room.cs`](filters/by-location/filter-by-room.cs) | Category narrowed to instances physically inside one room, matched by Id, Name, and/or Number — ✓ verified 2026-08-06 (a room was added to the test model for this: 4 of 5 air terminals matched, and the 5th is genuinely outside the wall loop at y=15862 vs the loop's 12862 top — so the count is proven by geometry, not assumed. Both lookup paths, and a bogus number takes the explicit "not found" path rather than a silent 0) |
| [`filter-by-solid-intersection.cs`](filters/by-location/filter-by-solid-intersection.cs) | Elements whose real geometry intersects a custom 3D box/clearance solid (`ElementIntersectsSolidFilter`) — live-verified 2026-07-22 |
| [`filter-by-space.cs`](filters/by-location/filter-by-space.cs) | Category narrowed to instances physically inside one MEP Space (not a Room), matched by Id, Name, and/or Number — ✓ verified 2026-07-22 (name-matching fix in header) |
| [`filter-by-unenclosed-spatial-elements.cs`](filters/by-location/filter-by-unenclosed-spatial-elements.cs) | QA sweep — every Room/Space in the model with zero Area ("Not Enclosed") — ✓ verified 2026-08-06 **both branches**: 0 unenclosed of 1 with only the placed room, then an UNPLACED room created in a transaction (1 of 2), scope filters checked, rolled back. `Document.Create.NewRoom(Phase)` is the unplaced-room call — `NewRoom(null, null)` is an ambiguous overload and will not compile |

**What it's ATTACHED TO — host, group, link, MEP system, insulation** — [`filters/by-relationship/`](filters/by-relationship/)
| Fragment | Job |
|---|---|
| [`filter-by-assembly.cs`](filters/by-relationship/filter-by-assembly.cs) | Member elements of a specific Revit Assembly (`AssemblyInstance`) — ✓ graceful path only 2026-07-22 (no Assembly fixture) — ✓ verified 2026-08-06 **by Id only** (assembly created in a transaction from the 3 ducts, all 3 members returned, rolled back). **The name path is unproven**: a freshly created `AssemblyInstance` has an empty `Name` until its type is named, and `AssemblyTypeName` throws "No valid type for the assembly instance" — so name lookup could not be exercised here |
| [`filter-by-connection-status.cs`](filters/by-relationship/filter-by-connection-status.cs) | Category elements with at least one open connector end, or fully connected — ✓ verified 2026-08-06 (**both branches**, 3 categories; open + fullyConnected + noConnectors summed exactly to the category total each time, and its 9 open-ended elements matched `filter-by-warnings.cs` independently) |
| [`filter-by-electrical-system.cs`](filters/by-relationship/filter-by-electrical-system.cs) | Elements in a specific Electrical System (circuit), by Circuit Type and/or circuit name — ✓ graceful path only 2026-07-22 (no electrical fixture) — ✓ verified 2026-08-14 — the "genuinely blocked, needs electrical content" call of 2026-08-07 was overturned by **building the content**: M_Duplex Receptacle + M_Lighting and Appliance Panelboard loaded from the stock MEP library, then `ElectricalSystem.Create(...PowerCircuit)` + `SelectPanel`. Type "Power" returned exactly the receptacle across 1 system; "Data" returned 0 as a negative control. Three gotchas recorded in the header: `.Elements` returns the LOADS not the feeding panel; only the **MEP** library families have electrical connectors (the Architectural ones have a NULL ConnectorManager); and `Connector.IsConnected` THROWS on a panelboard's CableTrayConduit connectors |
| [`filter-by-group.cs`](filters/by-relationship/filter-by-group.cs) | Member elements of a specific Model Group instance — ✓ verified 2026-08-06 (a named group was added to the test model for this: all three lookup paths — by Id, by name, by lowercase name — returned the same 5 members; a bogus name takes the explicit "not found" path) |
| [`filter-by-host.cs`](filters/by-relationship/filter-by-host.cs) | Elements hosted on a specific parent (`FamilyInstance.Host` or insulation/lining `HostElementId`) — ✓ graceful path only 2026-07-22 (no hosted fixture) — ✓ verified 2026-08-06 (a door was loaded and hosted in a wall for this: 1 hosted element found on wall 918932, and **0 on a different wall — a proven zero, since the same code found 1 next door**; category filter and a bogus host Id both behave) |
| [`filter-by-insulation-status.cs`](filters/by-relationship/filter-by-insulation-status.cs) | Pipe/duct elements that HAVE insulation/lining applied, or don't — ✓ graceful path only 2026-07-22 (no insulation fixture) — ✓ verified 2026-08-06, **both branches proven by changing the model**: bare 3 / insulated 0, then 25 mm Duct Wrap added inside a transaction (2 / 1), then rolled back (3 / 0, model untouched). Without the flip, "0 insulated" is indistinguishable from a filter that never matches |
| [`filter-by-insulation-type.cs`](filters/by-relationship/filter-by-insulation-type.cs) | The insulation/lining elements themselves, by kind/type/material/thickness — ✓ graceful path only 2026-07-22 (no insulation fixture) — ✓ verified 2026-08-06 by adding 25 mm Duct Wrap inside a transaction and rolling back: **every input exercised** — all four `kind` values (ductinsulation 1, pipeinsulation 0, ductlining 0), type-name match and a proven zero, thickness range 20-30 mm hit and 50-99 mm miss, and `resolveToHost` correctly returning the duct rather than the insulation |
| [`filter-by-linked-model-elements.cs`](filters/by-relationship/filter-by-linked-model-elements.cs) | Elements of a category INSIDE a specific linked RVT model, not the link instance itself — read-only composition only, see the file's own GOTCHA — ✓ verified 2026-08-06 (reads the LINKED document, not the host: 1 level inside `MEP_TestLink` against 2 in the host — **that different number is the proof** it crossed into the link rather than re-reading the host. A non-matching link name takes the explicit not-found path) |
| [`filter-by-links.cs`](filters/by-relationship/filter-by-links.cs) | Every RVT link and/or CAD link instance, optional name substring — feeds `action-set-workset.cs` ("move the links onto a workset") — ✓ verified 2026-08-06 (a small purpose-made `MEP_TestLink.rvt` was created and linked rather than touching any real project: **all three modes** rvt/cad/both — cad correctly 0 with no CAD imports present — plus name filtering and a proven zero) |
| [`filter-by-subcomponents.cs`](filters/by-relationship/filter-by-subcomponents.cs) | NESTED sub-components inside parent FamilyInstances (optionally recursive) — the members a category filter never finds; reverse direction of `filter-by-host.cs` — NOT yet live-verified (2026-07-26 round 3, Clockwork-equivalent) — graceful path ✓ verified 2026-08-07, positive path **still genuinely blocked**: a scan of every FamilyInstance in the model found 0 with sub-components, so there is nothing nested to find. Run against an AHU plus a duct it correctly reported `0 found under 2 parent(s) (recursive); 1 input Id(s) were not FamilyInstances` — the not-a-FamilyInstance branch and the empty-result-is-valid rule both hold. Needs a nested shared family loaded |
| [`filter-by-system-name.cs`](filters/by-relationship/filter-by-system-name.cs) | Pipes/ducts/fittings narrowed to one specific System instance's own name (e.g. "DXS 1") — ✓ verified 2026-07-23 (same UnionWith fix) |
| [`filter-by-system-type.cs`](filters/by-relationship/filter-by-system-type.cs) | Pipes/ducts/fittings narrowed by MEP System TYPE/classification (e.g. "CDP", "Supply Air") — ✓ verified 2026-07-23 (UnionWith fix, story in header) |

**Its role in DOCUMENTATION — views, sheets, schedules, tags** — [`filters/by-view-and-sheet/`](filters/by-view-and-sheet/)
| Fragment | Job |
|---|---|
| [`filter-by-elements-in-view.cs`](filters/by-view-and-sheet/filter-by-elements-in-view.cs) | Category narrowed to instances actually visible in a given view (any view, not just active) — live-verified 2026-07-23, zero bugs |
| [`filter-by-schedules.cs`](filters/by-view-and-sheet/filter-by-schedules.cs) | Every ViewSchedule, optional name substring — feeds `action-export-schedule-to-csv.cs`/`action-place-schedule-on-sheet.cs` — ✓ verified 2026-08-06 (1 real schedule found; **the `IsTemplate`/`<...>` exclusion is NOT proven** — this model had 0 of either, so that branch never ran) |
| [`filter-by-scope-box.cs`](filters/by-view-and-sheet/filter-by-scope-box.cs) | Every Scope Box, optional name substring — feeds `action-assign-scope-box-to-view.cs`; delete via `action-delete-elements.cs`, no dedicated fragment needed — re-checked 2026-08-07: **genuinely blocked**, the model contains 0 Scope Boxes (`OST_VolumeOfInterest`) and Revit 2020 has no API to create one, so there is no way to build the fixture either. Same blocker as `create-scope-box.cs` and the assign path of `action-assign-scope-box-to-view.cs` |
| [`filter-by-sheets.cs`](filters/by-view-and-sheet/filter-by-sheets.cs) | Every ViewSheet, optional sheet-number substring — live-verified 2026-07-23, zero bugs |
| [`filter-by-tag-status.cs`](filters/by-view-and-sheet/filter-by-tag-status.cs) | Category elements that ARE or ARE NOT tagged in a given view — ✓ verified 2026-08-06, **both branches proven by changing the model**: with nothing tagged it read 3 untagged / 0 tagged, a tag was then created inside a transaction (2 / 1), and the transaction rolled back (3 / 0, model untouched). Without that flip the "tagged" branch is indistinguishable from a filter that never matches. A bogus view Id takes the explicit not-found path |
| [`filter-by-views.cs`](filters/by-view-and-sheet/filter-by-views.cs) | Every View (not ViewSheet), optional ViewType + name filter — ✓ verified 2026-08-06 (2 of 35 View-class elements matched; **correctly excluded all 16 view templates and the 1 schedule** — the exclusion is the part worth proving) |
| [`filter-by-view-templates.cs`](filters/by-view-and-sheet/filter-by-view-templates.cs) | View Templates themselves, optional name filter + usage mode (all/used/unused) — makes templates composable with any action (rename, report, delete, ...) instead of needing a bespoke fragment — ✓ verified 2026-08-06 (16 templates; **used + unused summed exactly to all 16**, which is the check that proves the usage split rather than assuming it; name filter and a proven zero) |

**Project STATE — workset, phase, design option, pin, warnings, selection** — [`filters/by-status/`](filters/by-status/)
| Fragment | Job |
|---|---|
| [`filter-by-current-selection.cs`](filters/by-status/filter-by-current-selection.cs) | Whatever's currently selected in Revit — ✓ verified 2026-08-06 (selection was empty, 3 ducts were selected via `UIDocument.Selection.SetElementIds`, the fragment read exactly those 3 back, then the original selection was restored — proving it reads live UI state rather than returning a constant) |
| [`filter-by-design-option.cs`](filters/by-status/filter-by-design-option.cs) | Elements in a named Design Option, or the Main Model when left unset — ✓ Main Model path 2026-07-22; named-option path blocked (no Design Option exists, none creatable via API — see action-set-design-option.cs) — ✓ verified 2026-08-06 and **fixed live**: Revit names the primary option of EVERY set "Option 1 (primary)", so the old `FirstOrDefault` on name silently picked one of three and reported success. Now takes `designOptionSetName` and **refuses to guess** when a name is ambiguous, listing the candidate sets. Main Model path proven (3 ducts); the option-matching path resolves to the right Id per set, but no elements are assigned to any option in the fixture, so element-matching inside an option is still unproven |
| [`filter-by-phase.cs`](filters/by-status/filter-by-phase.cs) | Elements matching a named Phase Created and/or Phase Demolished, optional category scope — ✓ verified 2026-08-06 (3 ducts on "New Construction"; 2 phases present, and the check compared raw int Ids so it did not share the `ElementId` equality under test) |
| [`filter-by-pin-status.cs`](filters/by-status/filter-by-pin-status.cs) | Category elements that ARE or ARE NOT pinned — ✓ verified 2026-08-06 (**both branches**: pinned a duct inside a transaction, saw the filter flip to 1 pinned / 2 unpinned, then rolled back — proves it tracks real state, not that everything happened to be unpinned) |
| [`filter-by-selection-filter.cs`](filters/by-status/filter-by-selection-filter.cs) | Read back the actual elements behind an existing named Selection Filter, or re-evaluate a View Filter's rule in a given view — live-verified 2026-07-22, both branches |
| [`filter-by-warnings.cs`](filters/by-status/filter-by-warnings.cs) | Elements flagged by a current model warning, as an actionable set — ✓ verified 2026-08-06 (9 elements via a description filter, cross-checked against `filter-by-connection-status.cs`; **`errorsOnly` branch still unproven** — this model had 0 Error-severity warnings. Remember 3 of its 20 warnings named NO element, so a warning count and an element count are different numbers) |
| [`filter-by-workset.cs`](filters/by-status/filter-by-workset.cs) | Elements on one user workset, optional category scope — ✓ verified 2026-08-06 on a real workshared local file (Workset1 → 130 elements, "Shared Levels and Grids" → exactly 4 grids + 2 levels which is precisely what that workset is for; category scope and active-view scope both narrowed correctly; a bogus name takes the explicit "not found" path) |

### Actions (consume `elements`)
Grouped into subfolders under `actions/` by job — same grouping used whenever these are listed out loud.

**Color & Graphics** — [`actions/color-graphics/`](actions/color-graphics/)
| Fragment | Job |
|---|---|
| [`action-set-color-uniform.cs`](actions/color-graphics/action-set-color-uniform.cs) | One color (line + solid fill) on every element — ✓ verified 2026-08-07: set RGB(255,0,0) on projection line, cut line AND the solid surface fill, all three read back by `action-report-graphic-overrides.cs` on a separate pass — so the "both line and surface, not just line" promise in the header is confirmed, not assumed. Verified 2026-08-07 as one read-back chain on 3 ducts, each step proving the next: clean -> `no overrides`; colour -> RGB(255,0,0); +transparency -> 50%; +halftone -> True; +line style -> weight 5, pattern 11 (Dash); reset -> `no overrides` and weight back to -1. Whole chain rolled back. |
| [`action-color-by-group.cs`](actions/color-graphics/action-color-by-group.cs) | Distinct color per group, grouped by any parameter's actual value; palette/gradient/random/pastel/neon modes — random/pastel/neon hue-step evenly around the color wheel so groups are GUARANTEED visually distinct, not independently randomized — ✓ verified 2026-08-07: grouping 5 air terminals by Mark produced 5 groups of 1, and the read-back colours were **exactly the first five palette entries in order** — (230,25,75) (60,180,75) (0,130,200) (245,130,48) (145,30,180) — so the grouping, the ordering and the palette index all line up. Rolled back to no overrides |
| [`action-highlight-vs-rest.cs`](actions/color-graphics/action-highlight-vs-rest.cs) | Highlight `elements` in one color, gray out every OTHER element in the active view — ✓ re-verified 2026-08-07 on the current fixture (the 2026-07-15 note was a different model): highlighting 3 ducts gave `3 red, 56 grayed, 0 skipped`, and 3 + 56 = 59 matches an independent count of everything in the active view — so "the rest" really did mean the whole view. Ducts read back (255,0,0) and all 8 walls (128,128,128) |
| [`action-reset-graphic-overrides.cs`](actions/color-graphics/action-reset-graphic-overrides.cs) | Clear PER-ELEMENT color/fill overrides — ✓ verified 2026-08-07 against a genuinely dirty element rather than a clean one: after colour + transparency + halftone + line weight + line pattern were all stacked on 3 ducts, one reset cleared **every** one of them (report back to `no overrides`, line weight back to -1, pattern back to -1). Verified 2026-08-07 as one read-back chain on 3 ducts, each step proving the next: clean -> `no overrides`; colour -> RGB(255,0,0); +transparency -> 50%; +halftone -> True; +line style -> weight 5, pattern 11 (Dash); reset -> `no overrides` and weight back to -1. Whole chain rolled back. |
| [`action-report-graphic-overrides.cs`](actions/color-graphics/action-report-graphic-overrides.cs) | Read back current view-specific graphic overrides per element — line color, fill color, transparency, halftone; read-only — ✓ verified 2026-08-07 **with its zero proven**: `no overrides` is the model's default state, so it was made to flip — the same 3 ducts reported `no overrides`, then every applied override in turn, then `no overrides` again after reset. Its header note that `IsSurfaceForegroundPatternVisible` is not itself a signal of an override held up: it read True throughout, including while the element genuinely had none. Verified 2026-08-07 as one read-back chain on 3 ducts, each step proving the next: clean -> `no overrides`; colour -> RGB(255,0,0); +transparency -> 50%; +halftone -> True; +line style -> weight 5, pattern 11 (Dash); reset -> `no overrides` and weight back to -1. Whole chain rolled back. |
| [`action-set-transparency.cs`](actions/color-graphics/action-set-transparency.cs) | Set surface transparency (0-100%) — ✓ verified 2026-08-07: 50% read back as `transparency 50%`, and the pre-existing red override survived it untouched. Verified 2026-08-07 as one read-back chain on 3 ducts, each step proving the next: clean -> `no overrides`; colour -> RGB(255,0,0); +transparency -> 50%; +halftone -> True; +line style -> weight 5, pattern 11 (Dash); reset -> `no overrides` and weight back to -1. Whole chain rolled back. |
| [`action-set-category-transparency.cs`](actions/color-graphics/action-set-category-transparency.cs) | Set surface transparency for one or more ENTIRE categories — does NOT consume `elements` — ✓ verified 2026-08-07: 40% read back off the category, and the blue category colour already present survived it — the "existing category color preserved" claim holds. Verified 2026-08-07 as a category-level read-back chain: none -> blue + 40% transparency + halftone + weight 7 all stacked on Ducts at once -> reset -> none. Rolled back. |
| [`action-set-category-color.cs`](actions/color-graphics/action-set-category-color.cs) | Override one or more ENTIRE categories' line/fill color in a view (Visibility/Graphics > Model Categories) — does NOT consume `elements`, categories are a direct array input — ✓ verified 2026-08-07 and **fixed live (real, and easy to miss)**: `SetCategoryOverrides` silently discards part of what it is given, depending on `Category.IsCuttable`. On **Ducts, Pipes and Air Terminals only the projection line colour survives** — cut line and the whole surface fill are dropped — while Walls and Doors keep everything and Mechanical Equipment keeps the fill but not the cut line. The setters never complain and the in-memory object holds every value, so "colour the ducts blue" gave blue lines and no fill while the fragment reported the fill applied. It now reads the override back and names exactly what Revit discarded, with the pointer that a solid look on those categories needs an ELEMENT override instead. Re-proved on Ducts (3 dropped), Walls (nothing dropped) and a mixed run. Verified 2026-08-07 as a category-level read-back chain: none -> blue + 40% transparency + halftone + weight 7 all stacked on Ducts at once -> reset -> none. Rolled back. |
| [`action-reset-category-graphics.cs`](actions/color-graphics/action-reset-category-graphics.cs) | Clear one or more CATEGORY-level graphic overrides — the paired undo for `action-set-category-color.cs` — does NOT consume `elements` — ✓ verified 2026-08-07 against a category carrying four stacked overrides (colour + transparency + halftone + line weight): one reset cleared all of them and the report went back to `No category-level overrides found`. Verified 2026-08-07 as a category-level read-back chain: none -> blue + 40% transparency + halftone + weight 7 all stacked on Ducts at once -> reset -> none. Rolled back. |
| [`action-create-view-filter.cs`](actions/color-graphics/action-create-view-filter.cs) | Create a Revit VIEW FILTER (`ParameterFilterElement`, the Visibility/Graphics > Filters tab rule mechanism — NOT this repo's `filters/` folder) — persists, auto-applies to future elements too; every rule kind (contains/equals/begins/ends + not-variants, numeric eq/gt/gte/lt/lte/noteq, has-value/has-no-value) — does NOT consume `elements` — ✓ verified 2026-07-22 (contains-rule path) |
| [`action-create-selection-filter.cs`](actions/color-graphics/action-create-selection-filter.cs) | Save `elements` as a named Revit SELECTION FILTER (`SelectionFilterElement`) — an explicit element list instead of a rule, for when the set doesn't share one clean parameter condition — live-verified 2026-07-22 |
| [`action-apply-view-filter.cs`](actions/color-graphics/action-apply-view-filter.cs) | Add an existing filter (View Filter OR Selection Filter — looked up by the shared `FilterElement` base) to a view with a color/visibility, or update it if already applied — does NOT consume `elements` — ✓ verified 2026-08-07 across the whole lifecycle: a fresh Ducts-scoped filter applied with `visible: true` read back colour (255,0,0) with cut colour AND surface fill both set, then **re-running it with `visible: false` updated in place rather than erroring** (the safe-to-re-run claim) and took the view's visible duct count 3 -> 0. Not-found path reports the create-it-first message. **Worth knowing: filter overrides KEEP cut colour and surface fill on Ducts, where a category override on that same non-cuttable category silently discards them** — so a View Filter is the right tool for colouring MEP solidly, see [`../knowledge/live-model/graphic-override-precedence.md`](../knowledge/live-model/graphic-override-precedence.md) |
| [`action-remove-view-filter.cs`](actions/color-graphics/action-remove-view-filter.cs) | Take a filter (either kind) off a view, optionally delete it from the document entirely — does NOT consume `elements` — ✓ verified 2026-08-07 (non-destructive path): removed the filter from the view, the view's hidden ducts came back 0 -> 3, and the filter definition itself survived in the document exactly as the header promises. Not-found path reports "nothing to remove". **The `deleteFilterElement = true` path stays unrun** — note that the bridge's destructive guard trips on the mere presence of `Document.Delete` in the text even when that branch is unreachable, so this fragment always needs `allowDestructive: true` to run at all |
| [`action-set-halftone.cs`](actions/color-graphics/action-set-halftone.cs) | Turn halftone on/off per element — read-modify-write, preserves any existing color override — ✓ verified 2026-08-07, **including the read-modify-write claim its header makes**: applied over an element that already carried a red colour override and 50% transparency, halftone went True and both of those survived unchanged. Verified 2026-08-07 as one read-back chain on 3 ducts, each step proving the next: clean -> `no overrides`; colour -> RGB(255,0,0); +transparency -> 50%; +halftone -> True; +line style -> weight 5, pattern 11 (Dash); reset -> `no overrides` and weight back to -1. Whole chain rolled back. |
| [`action-set-category-halftone.cs`](actions/color-graphics/action-set-category-halftone.cs) | Turn halftone on/off for one or more ENTIRE categories — does NOT consume `elements` — ✓ verified 2026-08-07: halftone True read back off the category with the existing colour and transparency both intact. Verified 2026-08-07 as a category-level read-back chain: none -> blue + 40% transparency + halftone + weight 7 all stacked on Ducts at once -> reset -> none. Rolled back. |
| [`action-set-line-style.cs`](actions/color-graphics/action-set-line-style.cs) | Override line weight and/or line pattern (dashed, dotted, ...) per element — every other action here only ever touches color — ✓ verified 2026-08-07: weight 5 and pattern 'Dash' both read back (`ProjectionLineWeight=5`, `ProjectionLinePatternId=11`), and the colour, transparency and halftone already on those elements all survived — the read-modify-write claim holds here too. Verified 2026-08-07 as one read-back chain on 3 ducts, each step proving the next: clean -> `no overrides`; colour -> RGB(255,0,0); +transparency -> 50%; +halftone -> True; +line style -> weight 5, pattern 11 (Dash); reset -> `no overrides` and weight back to -1. Whole chain rolled back. |
| [`action-set-category-line-style.cs`](actions/color-graphics/action-set-category-line-style.cs) | Override line weight/pattern for one or more ENTIRE categories — does NOT consume `elements` — ✓ verified 2026-08-07: weight 7 and pattern 'Dot' applied at category level and read back as `projection weight 7`, with colour, transparency and halftone all surviving. Unlike the colour setter, line weight is NOT affected by the non-cuttable restriction — it stuck on Ducts. Verified 2026-08-07 as a category-level read-back chain: none -> blue + 40% transparency + halftone + weight 7 all stacked on Ducts at once -> reset -> none. Rolled back. |
| [`action-report-view-filters.cs`](actions/color-graphics/action-report-view-filters.cs) | List every View/Selection Filter in the document and which views use each — does NOT consume `elements` — ✓ verified 2026-07-23 (ProjectBrowser pseudo-view fix in header) |
| [`action-report-category-overrides.cs`](actions/color-graphics/action-report-category-overrides.cs) | Reverse lookup for `action-set-category-color.cs`/halftone/line-style — which categories have a category-level override set in a view — does NOT consume `elements` — ✓ verified 2026-08-07 **with its zero proven**: reported `No category-level overrides found ... among 15 categor(y/ies) checked` on the clean model, then correctly surfaced Ducts once overridden, then returned to none after reset. Its auto-scope (categories with elements actually present in the view) resolved to 15, not the whole document. It also independently exposed the partial-write above — it was this fragment reporting `cut line (none)` next to a set projection line that started the investigation. Verified 2026-08-07 as a category-level read-back chain: none -> blue + 40% transparency + halftone + weight 7 all stacked on Ducts at once -> reset -> none. Rolled back. |

**Visibility** — [`actions/visibility/`](actions/visibility/)
| Fragment | Job |
|---|---|
| [`action-isolate-elements.cs`](actions/visibility/action-isolate-elements.cs) | Temporary isolate, reset-then-apply — ✓ verified 2026-08-07 against what the view actually renders: isolating the 2 AHUs took the active view from ducts=3/equip=2/walls=8 to ducts=0/equip=2/walls=0, `IsTemporaryHideIsolateActive` False -> True, and rollback restored all three counts. `CanUseTemporaryVisibilityModes()` accepted the FloorPlan |
| [`action-hide-elements.cs`](actions/visibility/action-hide-elements.cs) | Hide (temporary by default, or permanent) — ✓ verified 2026-08-07, **both modes, and they are genuinely different**: temporary hide took visible ducts 3 -> 0 and set `IsTemporaryHideIsolateActive` True, while permanent hide took the same 3 -> 0 but left that flag False and set `IsHidden(view)` True on each element. Both rolled back cleanly |
| [`action-unhide-elements.cs`](actions/visibility/action-unhide-elements.cs) | Reverse a permanent hide — ✓ verified 2026-08-07 end to end, which needed the permanent hide first: after `HideElements` left all 3 ducts at `IsHidden=True` and invisible, this took them back to `IsHidden=False` and visible=3. Proving it required creating the condition it reverses, not just running it on already-visible elements |
| [`action-manage-named-set.cs`](actions/visibility/action-manage-named-set.cs) | Name a set of elements once, then re-select / isolate / hide / show it by that name later — the "park the beams while I run the ducts" job. STANDALONE (`list` | `create` | `add` | `remove` | `select` | `isolate` | `hide` | `show` | `delete`); `create` reads the current Revit selection, so pair it with [`action-create-selection-filter.cs`](actions/color-graphics/action-create-selection-filter.cs) when you want to save a filter chain's `elements` instead. Stores nothing in memory — it is built on Revit's own `SelectionFilterElement`, so the set lives in the model and every mode re-resolves the ids and reports how many went stale — not yet live-verified, compile-checked on Revit 2020/2024/2027 |
| [`action-show-elements.cs`](actions/visibility/action-show-elements.cs) | Zoom/show the filtered elements, optionally selecting them — ✓ verified 2026-08-07, all three branches: 3 ducts showed and the Revit selection really became [919024, 919034, 919044]; `maxElementsToShow=2` against 8 walls reported "(limited to first 2)" with a selection of exactly 2; an empty set reported "No elements to show" rather than throwing |
| [`action-set-view-crop.cs`](actions/visibility/action-set-view-crop.cs) | Crop the active view to fit the filtered element set + margin — ✓ verified 2026-08-07 **to the millimetre against a bounding box computed independently before the run**: 3 ducts + 500 mm gave min (-21301, 6947) max (4424, 16028), exactly as predicted, with `CropBoxActive` False -> True |
| [`action-section-box-and-zoom.cs`](actions/visibility/action-section-box-and-zoom.cs) | Section-box a 3D view around `elements` and zoom to them — ✓ verified 2026-08-07: found the `{3D}` view by name as designed and set a box matching the pre-computed expectation on all three axes, min (-21801, 6447, 1600) max (4924, 16528, 3900), `IsSectionBoxActive` False -> True. **Header now warns that the active-view switch does NOT roll back** — the section box does, the view the user is looking at does not |
| [`action-set-pin-state.cs`](actions/visibility/action-set-pin-state.cs) | Pin or unpin the filtered element set (same "reversible display/protection toggle" class as the rest of this group) — ✓ verified 2026-08-07 with all three counters proven: 3 ducts went Pinned False -> True (`updated 3`), an immediate re-run reported `unchanged 3, updated 0` (idempotent, not double-counting), and the 5 grouped air terminals reported `skipped 5` because Revit refuses to pin or unpin a group member at all ("Element cannot be pinned or unpinned") — caught by the try/catch rather than thrown |
| [`action-set-category-visibility.cs`](actions/visibility/action-set-category-visibility.cs) | Turn one or more ENTIRE categories on/off in a view (Visibility/Graphics > Model Categories checkbox) — does NOT consume `elements` — ✓ verified 2026-08-07 with the zero proven both ways: turning Ducts OFF took the view's visible duct count 3 -> 0, turning it back ON restored 3. Distinct from the per-element hide, as the header claims — this is the category checkbox, not an element property |
| [`action-report-category-visibility.cs`](actions/visibility/action-report-category-visibility.cs) | Which categories are currently OFF in a view — the reverse lookup for `action-set-category-visibility.cs` — does NOT consume `elements` — ✓ verified 2026-08-06 (50 categories in the active view; correctly listed the 5 already OFF and the 31 that do not allow visibility control. **The OFF branch was proven by changing the model**: Ducts hidden inside a transaction appeared in the OFF list, then rolled back) |
| [`action-set-view-workset-visibility.cs`](actions/visibility/action-set-view-workset-visibility.cs) | Make one view show ONLY named workset(s) — the "Workset 3D View" pattern, every other user workset turned off in that view; does NOT consume `elements` — BLOCKED (model isn't workshared); graceful path ✓ |
| [`action-assign-scope-box-to-view.cs`](actions/visibility/action-assign-scope-box-to-view.cs) | Assign (or clear) a named Scope Box as a view's own Scope Box property — ✓ clear-mode 2026-07-22; assign path blocked (no Scope Box can exist — see create-scope-box.cs) — re-checked 2026-08-07: **still genuinely blocked** for the assign path. The model contains 0 Scope Boxes and none can be created through the API on this Revit version, so only the clear path (verified 2026-07-22) and the not-found path (verified 2026-08-07 — reports the name and an empty available list rather than throwing) have ever run |
| [`action-set-crop-box-settings.cs`](actions/visibility/action-set-crop-box-settings.cs) | Turn Crop Region on/off, its boundary visibility on/off, and/or Annotation Crop on/off across views — independent flags, pairs with `action-set-view-crop.cs` for resizing — ✓ verified 2026-08-07: run on a mixed list (1 view + 1 duct) it set CropBoxActive=True, CropBoxVisible=False and AnnotationCrop=1 on the view and correctly reported `1 non-View element(s) skipped`. **The Revit-2020 AnnotationCrop route is now proven at runtime, not just at compile time** — `VIEWER_ANNOTATION_CROP_ACTIVE` was the 2026-08-04 fix for a property that does not exist on 2020, and this is the first run that actually exercised it |
| [`action-set-view-range.cs`](actions/visibility/action-set-view-range.cs) | Report or set a plan view's View Range — cut plane, top, bottom, view depth, each as level + mm offset; the "why don't my ducts show" fix; report-mode first (plane-order rule + ceiling-plan gotcha in header); does NOT consume `elements` — ✓ report mode verified 2026-07-26 (4 plan views; a real sentinel-level bug was found and fixed that run); set mode unexercised |

**Selection** — [`actions/selection/`](actions/selection/)
| Fragment | Job |
|---|---|
| [`action-select-elements.cs`](actions/selection/action-select-elements.cs) | Set, add to, or remove from the active Revit selection (`mode`) — ✓ verified 2026-08-07, all three modes against the real Revit selection count: replace 3 ducts -> 3; add 8 walls -> 11; **add the same 8 again -> still 11**, proving the HashSet genuinely de-duplicates rather than double-counting; remove the 3 ducts -> 8; replace with 5 terminals -> 5 with the walls gone |

**Parameters & Naming** — [`actions/parameters-naming/`](actions/parameters-naming/)
| Fragment | Job |
|---|---|
| [`action-set-parameter-value.cs`](actions/parameters-naming/action-set-parameter-value.cs) | Bulk-set one parameter across the set — falls back to the Type if it's not an instance parameter — ✓ verified 2026-08-07 across every branch: instance String write (3 ducts), TYPE-level write (reported separately as `3 at Type level` — and the shared-type warning is real, one write changed `Type Comments` for all three), missing name -> skipped 3, read-only `Length` -> skipped 3, and **the default INPUTS with both value fields null correctly do nothing** rather than blanking anything. **Fixed live 2026-08-07 along with the rest of this group**: `Parameter.Set()` returns a bool and Revit uses it to REFUSE a value without throwing — `Set(0.0)` on a duct's Width returns false and leaves it at 300 mm. The fragment counted the write as done. It now counts only what Revit accepted and names the refused element Ids. See [`../knowledge/live-model/core.md`](../knowledge/live-model/core.md). Re-proved: Width=0 mm -> `0 updated, 3 REFUSED`; Width=450 mm -> `3 updated` and the model reads 450 |
| [`action-add-parameter-prefix-suffix.cs`](actions/parameters-naming/action-add-parameter-prefix-suffix.cs) | Add a prefix/suffix, or find/replace a substring, INSIDE a parameter's existing text (any String parameter, any category) — falls back to Type, deduped so a shared type isn't stacked — ✓ verified 2026-07-22 |
| [`action-add-project-parameter.cs`](actions/parameters-naming/action-add-project-parameter.cs) | Create a NEW shared-parameter-backed project parameter and bind it to categories — different job from every other fragment here, which only edit values of parameters that already exist; does NOT consume `elements` — ✓ verified 2026-07-22, Revit-2020 legacy API (version note in header) |
| [`action-find-replace-element-name.cs`](actions/parameters-naming/action-find-replace-element-name.cs) | Find/replace, prefix, or suffix `Element.Name` itself — works on ANY nameable element (Room, Sheet, View, Level, Grid, Group, Material, Type...), just pair with the matching filter — ✓ verified 2026-07-22 |
| [`action-copy-parameter-value.cs`](actions/parameters-naming/action-copy-parameter-value.cs) | Copy one parameter's value into a different parameter, storage-type-aware — source and target each independently fall back to Type — ✓ verified 2026-08-07: Comments -> Mark copied on 3 ducts with the value read back identical, and re-running with `overwriteExisting = false` correctly skipped all 3 now that the target was populated. **Fixed live 2026-08-07 along with the rest of this group**: `Parameter.Set()` returns a bool and Revit uses it to REFUSE a value without throwing — `Set(0.0)` on a duct's Width returns false and leaves it at 300 mm. The fragment counted the write as done. It now counts only what Revit accepted and names the refused element Ids. See [`../knowledge/live-model/core.md`](../knowledge/live-model/core.md). |
| [`action-remove-parameter-value.cs`](actions/parameters-naming/action-remove-parameter-value.cs) | Clear one parameter's value — genuinely empty for String/ElementId, zeroed (not truly unset) for Double/Integer — falls back to Type — ✓ verified 2026-08-07 and **this is the fragment that exposed the `Set()` bug**: clearing String `Comments` genuinely emptied it, but clearing Double `Width` reported `zeroed on 3` while all three ducts stayed 300 mm wide — Revit refuses 0 for a duct. **Fixed live 2026-08-07 along with the rest of this group**: `Parameter.Set()` returns a bool and Revit uses it to REFUSE a value without throwing — `Set(0.0)` on a duct's Width returns false and leaves it at 300 mm. The fragment counted the write as done. It now counts only what Revit accepted and names the refused element Ids. See [`../knowledge/live-model/core.md`](../knowledge/live-model/core.md). Re-proved: the same run now reports `0 zeroed, 3 REFUSED` with the Ids. The header's honesty note (Double/Integer can only be zeroed, never truly unset) still holds and is now backed by a second one — sometimes not even zeroed |
| [`action-renumber-sequential.cs`](actions/parameters-naming/action-renumber-sequential.cs) | Assign a sequential value (prefix/number/padding/suffix) to a String parameter, sorted by position or existing value — ✓ verified 2026-08-07 twice over: `position_x` on 5 air terminals produced AT-101..AT-105 in true left-to-right X order (-13405 -> 3295 mm), and `position_y` with increment 10, prefix Z, suffix -A and pad 2 produced Z01-A..Z41-A top-of-plan first — so sort mode, start, increment, prefix, suffix and zero-padding are each confirmed, not assumed. **Fixed live 2026-08-07 along with the rest of this group**: `Parameter.Set()` returns a bool and Revit uses it to REFUSE a value without throwing — `Set(0.0)` on a duct's Width returns false and leaves it at 300 mm. The fragment counted the write as done. It now counts only what Revit accepted and names the refused element Ids. See [`../knowledge/live-model/core.md`](../knowledge/live-model/core.md). It matters most here: Room and Sheet Number must be unique, so a clash is refused per element and would otherwise be reported as a clean sequence that has gaps |
| [`action-rename-element.cs`](actions/parameters-naming/action-rename-element.cs) | Rename each element via `Element.Name` (views, sheets, levels, types — not most instance geometry); live-verified 2026-07-22, zero bugs |
| [`action-rename-family.cs`](actions/parameters-naming/action-rename-family.cs) | Bulk-rename the FAMILY behind a set of instances (resolves instance → Symbol → Family, dedupes so each family is renamed once) — prefix, suffix, find/replace, or flat replace modes — ✓ verified 2026-07-22 (renamed + restored a real family) |
| [`action-create-phase.cs`](actions/parameters-naming/action-create-phase.cs) | Create new project Phases — does NOT consume `elements` — CONFIRMED IMPOSSIBLE on Revit 2020 (`Document.Phases` is read-only; UI-only, Manage > Phases) — fragment reports this instead of throwing |
| [`action-rename-phase.cs`](actions/parameters-naming/action-rename-phase.cs) | Rename existing project Phases — does NOT consume `elements` — CONFIRMED IMPOSSIBLE on Revit 2020 (Phase Name is read-only; UI-only, Manage > Phases) — fragment reports this instead of throwing |
| [`action-set-element-phase.cs`](actions/parameters-naming/action-set-element-phase.cs) | Assign the filtered set's Phase Created and/or Phase Demolished to a named Phase — `action-set-parameter-value.cs` can't do this (no ElementId support) — ✓ verified 2026-08-07, every branch: setting Phase Created='Existing' and Phase Demolished='New Construction' on 3 ducts reported `set on 3 / set on 3 / skipped 0` and both read back off the elements; a phase name that does not exist reports it and changes nothing; and calling it with neither name set reports "No phase specified" rather than silently doing nothing. Rolled back to Created='New Construction', Demolished=none |
| [`action-report-phases.cs`](actions/parameters-naming/action-report-phases.cs) | List every project Phase in order — does NOT consume `elements` — ✓ verified 2026-08-07: listed the model's 2 phases in order with Ids — 'Existing' (12589) then 'New Construction' (86961) — which is the discovery step the phase-rename/delete/set fragments need |
| [`action-delete-phase.cs`](actions/parameters-naming/action-delete-phase.cs) | Permanently delete Phases by name — completes the phase lifecycle — does NOT consume `elements` — ✓ verified 2026-07-22 (rollback-probe technique, detail in header) |
| [`action-set-workset.cs`](actions/parameters-naming/action-set-workset.cs) | Assign elements to a named user workset (write counterpart to `filter-by-workset.cs`) — BLOCKED (model isn't workshared); graceful path ✓ |
| [`action-set-design-option.cs`](actions/parameters-naming/action-set-design-option.cs) | Add elements to a named Design Option via the copy-while-active-option workaround (write counterpart to `filter-by-design-option.cs`) — Revit 2020 has NO API to activate a Design Option, so the option must be activated manually first (story in header) — graceful paths ✓ 2026-07-22, success path blocked — **both guard paths ✓ verified 2026-08-07; the copy itself still cannot be run.** The old note said this was blocked because the model had 0 Design Options — **it has 6 now** (3 sets, built as a fixture), so that reason is retired and the header is corrected. The real remaining blocker is narrower: the copy only lands in an option while that option is ACTIVE, and no script can activate one. Re-confirmed by reflection on this build that the only member matching 'ActiveDesignOption' on `DesignOption` or `Document` is the read-only `GetActiveDesignOptionId` — there is still no setter. Verified: the not-found guard lists the 6 real options by name, and the not-active guard refuses with the exact Revit UI steps instead of copying into the wrong option. **To finish: make an option active in Revit, then re-run** |
| [`action-import-parameters-from-csv.cs`](actions/parameters-naming/action-import-parameters-from-csv.cs) | Read a CSV (ElementId + parameter columns — the shape `action-export-parameters-to-csv.cs` writes) and bulk-set the values back — write half of the Excel round-trip; does NOT consume `elements` (rows resolve by Id); explorer-first applies — NOT yet live-verified (2026-07-26 gap backlog) |
| [`action-rename-workset.cs`](actions/parameters-naming/action-rename-workset.cs) | Rename a user Workset (`WorksetTable.RenameWorkset`); workset DELETE is CONFIRMED IMPOSSIBLE on 2020 (API is 2022+) — reported, not thrown; does NOT consume `elements` — BLOCKED (not workshared), — ✓ verified 2026-08-07, **no longer blocked** (the 2026-07-26 "model isn't workshared" note is retired — the fixture is a workshared local now). All four paths ran: `delete` mode returns the honest Revit-2020-impossible message with the manual UI steps rather than pretending; an unknown name lists the real worksets; an empty newWorksetName is refused; and the rename took 'Workset1' -> 'MEP_VerifyWorkset', confirmed by re-reading the workset table, then rolled back |
| [`action-find-replace-text-notes.cs`](actions/parameters-naming/action-find-replace-text-notes.cs) | Find (report) or find-and-replace inside TextNote TEXT — the words on the drawing, not parameter values; find-first then replace per the explorer-first rule — ✓ verified 2026-08-07 against 3 purpose-made notes ('Supply duct SD-01...', 'SUPPLY riser SD-02', 'Return air only'). **The case flag genuinely works both ways**: searching 'supply' case-INsensitively hit 2 of 3, and case-SENSITIVELY hit 0 of 3 — correct, since neither note contains lowercase 'supply'. `find` mode reports each hit with its Id and owning view and changes nothing; `replace` mode changed exactly 2 of 3 ('SD-' -> 'MEP_SD-', read back off the notes) and left the third untouched; an empty `findText` is refused. The hand-rolled case-insensitive replace preserves the surrounding text correctly |

**Reporting** — [`actions/reporting/`](actions/reporting/)
| Fragment | Job |
|---|---|
| [`action-count-and-report.cs`](actions/reporting/action-count-and-report.cs) | Bare count or size-breakdown table — ✓ verified 2026-08-06 composed after `filter-by-category` (3 ducts; `wantBreakdownTable` with `preferredParamName="Height"` gave the 300mm row, exercising the preferred-parameter reorder the header warns about) |
| [`action-count-by-group.cs`](actions/reporting/action-count-by-group.cs) | Count broken down by ANY parameter's value (Level, System Type, Family, Comments, Phase Created, ...) — the general case beyond size — ✓ verified 2026-08-06 and **fixed live**: grouping by `"Level"` returned `None` for ducts, walls and pipes, because a Duct's parameter is called **"Reference Level"** and a Wall has no level-named parameter at all — only an air terminal has a literal `"Level"`, which is why this looked fine. Now uses the same fallback chain as the filters. All keys re-checked: Family, Family and Type, Category, Level, Comments, Width |
| [`action-count-by-spatial-container.cs`](actions/reporting/action-count-by-spatial-container.cs) | Count broken down by which Room/Space/Zone physically contains each element — spatial test, not a parameter lookup (Room/Space have no such parameter on most MEP elements) — ✓ verified 2026-08-06 (**all three container modes**: room gave 4 in 'MEP Test Room T01' + 1 '(no room)', matching `filter-by-room` exactly by a different code path; space and zone correctly no-op to '(no space)' on a model with none, rather than crashing) |
| [`action-report-parameters.cs`](actions/reporting/action-report-parameters.cs) | Parameter table (values) for parameter names you already know — level chain fixed 2026-08-06 (same `RBS_START_LEVEL_PARAM` gap — MEP curves reported a blank level) — ✓ verified 2026-08-07 and **fixed live**: a parameter that does not EXIST printed the same empty cell as a parameter that exists and is blank, which is the exact conflation [`../knowledge/live-model/core.md`](../knowledge/live-model/core.md) calls a silent wrong answer. Now prints `(no such parameter)` (new `markMissingParameters` input, same wording as `lib/prelude.cs` ParamText), and the type-parameter fallback no longer discards a blank-but-present instance parameter. Zero proven by setting Mark/Comments inside a transaction and rolling back — blank -> value -> blank; `maxRows` truncation fired at 2 of 3; ducts' Level resolved to Level 1 via the RBS chain |
| [`action-report-parameter-inventory.cs`](actions/reporting/action-report-parameter-inventory.cs) | Discover what parameters an element actually HAS — name, kind (Built-in/Shared/Project-Family), group, storage type, Instance vs Type, read-only, value — before you know the names to ask for — ✓ verified 2026-08-06 (61 distinct parameters off one air terminal — 33 Instance + 28 Type; the Kind classifier split 51 Built-in from 10 Project/Family, and the header's honesty note about that third bucket holds: none reported as Shared because none are. `maxRows` truncation fired) |
| [`action-report-location.cs`](actions/reporting/action-report-location.cs) | Report each element's position (point, line endpoints, or bounding-box-center fallback); read-only — ✓ verified 2026-08-06 (**all three branches**: LocationPoint on terminals, LocationCurve on ducts, and the bounding-box fallback path identified; `maxRows` truncation notice fired correctly at 3 of 5) |
| [`action-report-bounding-box.cs`](actions/reporting/action-report-bounding-box.cs) | Report each element's bounding box + the combined extents of the set; read-only — ✓ verified 2026-08-06 (per-element sizes against known geometry, `maxRows` truncation notice at 2 of 4, and the combined-extents accumulator correct) |
| [`action-report-material-takeoff.cs`](actions/reporting/action-report-material-takeoff.cs) | Material area/volume quantities across `elements`, grouped by material — ✓ verified 2026-08-07 against a hand calculation: 8 walls, all one material, summed to 644.30 m2 / 128.68 m3 where adding the 8 per-element `GetMaterialArea`/`GetMaterialVolume` values by hand and converting gives 6935.18 ft2 / 4544.12 ft3 = 644.30 m2 / 128.68 m3. `skipped` correctly 0 (every wall has material data) |
| [`action-report-length-by-size.cs`](actions/reporting/action-report-length-by-size.cs) | Count + total length per size group, for linear MEP elements (duct/pipe/cable tray) — ✓ verified 2026-08-06 **to the millimetre**: 3 ducts at 18234+4700+6150 = 29.08 m plus a 5.00 m pipe = 34.08 m total, matching a hand calculation. The round pipe arrived as `50 mmø` and **sorted at 50, ahead of 300x300** — proving the ASCII-only sort key survives the diameter symbol its own header warns about |
| [`action-report-room-space-data.cs`](actions/reporting/action-report-room-space-data.cs) | Area/Volume/Level/Occupancy table for Rooms or Spaces; read-only — ✓ verified 2026-07-23 (ROOM_VOLUME fix in header) |
| [`action-compare-elements.cs`](actions/reporting/action-compare-elements.cs) | Side-by-side parameter diff of 2-8 elements — only differing values by default, instance + [T] type params; read-only — ✓ verified 2026-07-26 (3 walls: surfaced the 3 differing params, suppressed ~50 identical) |
| [`action-export-parameters-to-csv.cs`](actions/sheets-views/action-export-parameters-to-csv.cs) | Write chosen parameters of `elements` to a CSV (ElementId first column) — Revit half of the Excel round-trip, agent converts CSV↔xlsx outside Revit; paired with `action-import-parameters-from-csv.cs` — NOT yet live-verified (2026-07-26 gap backlog) |
| [`action-report-element-ownership.cs`](actions/reporting/action-report-element-ownership.cs) | Worksharing ownership per element — checkout status, creator, current owner, last changed by (the Worksharing tooltip in bulk); read-only — ✓ verified 2026-08-07, **no longer blocked** (the test model is workshared now, so the 2026-07-26 BLOCKED note is retired). **Both count branches proven, not just the easy one**: 8 walls gave 0 free / 8 owned by me, and 70 Materials gave 67 free / 3 owned by me — a genuinely mixed set, where the `NotOwned` rows print `owner: (none)` as designed. `maxRows` cap fired at 50 of 70. The `owned by OTHERS` branch stays unproven — it needs a second user on a real central file, which one machine cannot fake |
| [`action-report-coverage.cs`](actions/reporting/action-report-coverage.cs) | How much floor each element serves — radius + area per element, min/max/average/total, optional coverage circles drawn as detail arcs and grouped, optional write-back to a parameter. **There is usually no coverage parameter to read** (checked: the standard supply diffuser has none), so pick the source deliberately — `spacing` (half the gap to the nearest neighbour, pure geometry, best default), `flow` (needs YOUR L/s per m2 and refuses to invent one), `fixed` (a stated radius). The three disagree ~6x; header explains why, and why ~45% floor coverage is normal not a fault — ✓ verified 2026-07-26 (17 terminals: radii 1883/2492/3520 mm, 344.1 m2 total) |
| [`action-report-nearest-elements.cs`](actions/reporting/action-report-nearest-elements.cs) | Nearest element(s) from each source to a target set — one category, several, or a fixed Id list. Three metrics that give different answers on purpose: `gap` (real clearance between bounding boxes, best default), `centre` (straight line), `manhattan` (orthogonal, cable-realistic). Answers "which FCU serves this terminal", "nearest exit", "nearest light"; groups by target so you see the zoning — ✓ verified 2026-07-26 (17 terminals -> 6 FCUs, 4/4/3/2/2/2) |
| [`action-plan-shortest-route.cs`](actions/reporting/action-plan-shortest-route.cs) | Cheapest way to CONNECT a set — `tree` = Prim's minimum spanning tree (branching, provably least total, the real homerun shape), `chain` = single unbranched daisy-chain (nearest-neighbour + 2-opt, heuristic not proven optimal), **`continuous` = one run that FINISHES each group before moving on, jumping from a room's optimised exit to the genuinely nearest element in any room not yet done — the user's method and the best of the three on real geometry (109.0 m vs 134.3 m for independent groups + feeders, within ~2% of the unbuildable unconstrained minimum)**. **`groupBy`** (none/room/space/level/parameter) routes each room or zone as its own run — without it the algorithm doesn't know walls exist and hops into the next room and back (measured: 6 zone crossings where 2 would do). `connectGroups` adds feeders between groups. Manhattan metric by default because cable runs orthogonally; `drawRoute` picks DETAIL lines for a plan view and model lines otherwise. Point-to-point estimate, NOT a routed cable schedule — header is explicit, and explains why grouping reports a LONGER total (134.3 m vs 106.8 m) yet is the buildable answer — ✓ verified 2026-07-26 (17 terminals: tree 111.7 m vs chain 132.9 m manhattan; grouping paths smoke-tested, room branch needs placed rooms) |
| [`action-report-ray-hits.cs`](actions/reporting/action-report-ray-hits.cs) | Fire rays out of each element and report WHAT THEY HIT — element, category, distance in mm; up/down/sideways/plan-diagonals or all 26 cube directions at once; target category is a per-request input (Ceilings, Floors/slab, Walls...) or blank for nearest-anything. The LOOK step before `action-move-to-ray-hit.cs` — ✓ verified 2026-07-26 (a FindNearest self-hit bug was caught and fixed that run; diagonal distances cross-check to axis x root-2) |
| [`action-test-view-filter-match.cs`](actions/reporting/action-test-view-filter-match.cs) | Dry-run an existing View Filter against `elements` WITHOUT applying it to any view — the "why is my filter not catching these ducts?" tool. Three verdicts, not two: MATCH, in-scope-but-rule-not-satisfied, and **N/A because the element's category was never in the filter's scope**. That third one is the point — a plain pass/fail reports those as failures and sends you rewriting a rule that was already correct. Handles rule-based and selection filters; prints a DIAGNOSIS naming the missing categories — not yet live-verified, compile-checked on Revit 2020/2024/2027 |
| [`action-report-connectors.cs`](actions/reporting/action-report-connectors.cs) | Every MEP connector per element — domain, shape, size, origin, facing (BasisZ), REAL connected partners (not just the IsConnected flag) — packages step 1 of the user's connection method (`../knowledge/live-model/hvac-ducts.md`); optional open-only mode — reads are the live-proven core of the connect recipe, fragment itself NOT yet run (2026-07-26 round 3, MEPover-equivalent) — ✓ verified 2026-08-06 **both modes**, and it cross-checks: the open-only run named ducts 919024 and 919044, exactly the two `filter-by-connection-status` found by a completely different code path. Connector size, origin, facing direction and the `IsConnected` flag all reported |
| [`action-report-compound-structure.cs`](actions/reporting/action-report-compound-structure.cs) | Layer build-up of wall/floor/roof/ceiling TYPES — function, material, thickness per layer, core marks, total; deduped per type; read-only — ✓ verified 2026-07-26 (9 walls -> 1 type, core layer marked) |
| [`action-report-space-airflow.cs`](actions/reporting/action-report-space-airflow.cs) | Schedule of MEP Spaces — Number/Name/Level/Area/Volume and Design vs Actual Supply/Return/Exhaust, in metric; flags the three silent traps (unplaced Spaces with Area 0, Spaces with no Design figure for Actual to be checked against, and every Volume reading 0 when the project has Area and Volume Computations set to "Areas only"). Read-only; the WRITE counterpart is `recipes/set-space-airflow.cs` — ✓ verified live 2026-08-13 on a 27-Space model — 763.6 m², and **two of the three notes fired**: 27 of 27 with no Design figure, and every Volume 0. The unplaced-Space note correctly stayed silent because all 27 are placed, so that branch is the one still unproven |
| [`action-report-room-boundaries.cs`](actions/reporting/action-report-room-boundaries.cs) | Room/Space boundary loops as mm segments + the wall behind each segment (finish or centerline); outer loop vs holes marked; read-only — geometry feed for `create-wall.cs`/`create-line.cs` — ✓ graceful path verified 2026-07-26 (3 unplaced rooms -> honest "0 loops"); positive path still needs an enclosed room |

**QA Checks** — [`actions/qa-checks/`](actions/qa-checks/)
| Fragment | Job |
|---|---|
| [`action-find-duplicates.cs`](actions/qa-checks/action-find-duplicates.cs) | QA check — flag elements whose insertion points sit within a tolerance of each other (duplicate LOCATION); read-only, optional select — ✓ verified 2026-08-07, zero proven TWICE over: raising tolerance alone from 50 mm to 6000 mm (no model change) flagged exactly the one real 5936 mm pair, and stacking two AHUs inside a transaction took 2 MechEquip from 0 clusters to 1 (gap 7857 -> 0.0 -> 7857 mm on rollback). `LocationCurve` branch exercised on ducts (midpoints via `Evaluate(0.5)`). **A first attempt to flip this zero failed misleadingly** — see the pinned-group gotcha in [`../knowledge/live-model/geometry-and-transforms.md`](../knowledge/live-model/geometry-and-transforms.md): the fragment was right, the test move was silently ignored |
| [`action-find-duplicate-values.cs`](actions/qa-checks/action-find-duplicate-values.cs) | QA check — flag elements sharing the same value in a named parameter, e.g. duplicate Mark (duplicate DATA, not location); read-only, optional select — ✓ verified 2026-08-07 with the zero proven: 5 air terminals carrying distinct Marks 1-5 gave 0 duplicate values, then setting two of them to the same string inside a transaction gave exactly 1 group ('SAME': 919145, 919179) and 2 flagged, and rollback returned it to 0. `selectFlagged` genuinely changed the Revit selection. **Instance parameters only by design** — it does NOT fall back to the Type the way `action-find-blank-parameter.cs` does, because every instance of one type shares its type parameters, so a type-parameter run would flag the whole set as duplicates |
| [`action-find-blank-parameter.cs`](actions/qa-checks/action-find-blank-parameter.cs) | QA check — flag elements where a named parameter is blank/unset (falls back to Type); read-only, optional select — ✓ verified 2026-08-07, **both counts proven**: 5 air terminals with Marks 1-5 gave `0 blank, 0 don't even have this parameter`; blanking two inside a transaction gave `2 blank` and rollback restored `0`; asking for a name that doesn't exist gave `0 blank, 5 don't even have this parameter`, which is the distinction that makes this fragment trustworthy where a plain parameter report is not. A **whitespace-only** value ("   ") correctly counts as blank, not as data |
| [`action-check-surface-fit.cs`](actions/qa-checks/action-check-surface-fit.cs) | QA before/after snapping to a surface — fires rays from each element's FOOTPRINT (centre + corners, or 3x3) and flags STRADDLING two surfaces, OVERHANGING an edge, UNEVEN across the footprint, or SLOPED (can't sit flush). Reports BY EXCEPTION (summary + only the failures) so it scales to thousands; ~0.07 ms/ray measured — ✓ verified 2026-07-26, both the clean path (17 terminals OK) and the detection path (a boundary-parked terminal correctly caught as OVERHANGING where a single centre ray was confidently wrong) |
| [`action-report-clashes.cs`](actions/qa-checks/action-report-clashes.cs) | Basic clash report — real geometry intersection between `elements` (set A) and a second category (set B); read-only — ✓ verified 2026-07-22 |

**Move / Copy / Rotate** — [`actions/move-copy-rotate/`](actions/move-copy-rotate/)
| Fragment | Job |
|---|---|
| [`action-move-elements.cs`](actions/move-copy-rotate/action-move-elements.cs) | Translate every element by one mm offset vector — ✓ verified 2026-08-07 and **fixed live (real bug)**: it counted a move as done whenever `MoveElement` didn't throw, and `MoveElement` returns normally while moving NOTHING on a pinned element or group member. Asked to shift 5 grouped air terminals 1000 mm it reported `Moved 5 element(s), skipped 0` with every coordinate unchanged. Now records each position, re-reads it after `Regenerate()`, and reports `moved` / `partial` (constrained by a host) / `blocked` (named ids + the reason) / `skipped` / `unverified` from the geometry itself. Re-proved: 5 grouped terminals -> `Moved 0` + 5 named as blocked; 3 ungrouped ducts -> `Moved 3`, all exactly +1000 mm; a 0 mm offset still reports moved, not blocked. See [`../knowledge/live-model/geometry-and-transforms.md`](../knowledge/live-model/geometry-and-transforms.md) |
| [`action-copy-elements.cs`](actions/move-copy-rotate/action-copy-elements.cs) | Duplicate every element, offset by one mm vector; produces `newElementIds` for chaining — ✓ verified 2026-08-07, **no fix needed** and that is the interesting part: its count comes from the ids `CopyElement` actually returned, not from "didn't throw", so it cannot tell the lie its move/rotate siblings told. 3 ducts -> 3 new (count 3 -> 6), and **5 pinned group members -> 5 new (count 5 -> 10) — copy WORKS on group members where move and rotate silently no-op** |
| [`action-rotate-elements.cs`](actions/move-copy-rotate/action-rotate-elements.cs) | Rotate every element around a vertical axis by one angle, about its own location or a given pivot — ✓ verified 2026-08-07 and **fixed live (same bug as move)**: `RotateElement` also returns normally while rotating nothing on a pinned/grouped element — 5 grouped terminals reported `Rotated 5` at an unchanged `LocationPoint.Rotation` of 0.00 degrees. Now compares an orientation signature before/after and reports `blocked` with named ids. Re-proved across all five cases: grouped terminals -> `Rotated 0` + 5 blocked; 2 AHUs -> 0.00 -> 90.00 deg; 3 ducts via the LocationCurve midpoint pivot -> direction (1.00, 0.09) -> (-0.09, 1.00), exactly 90 degrees; and 0 deg / 360 deg correctly report rotated rather than blocked |
| [`action-mirror-elements.cs`](actions/move-copy-rotate/action-mirror-elements.cs) | Mirror every element across a vertical plane through two plan points — copy or in-place — ✓ verified 2026-08-07 (both modes + the degenerate-axis guard). `mirrorCopy = true` is exact: 3 ducts -> 3 new at precisely negated Y (-8391 / -12014 / -15111 mm), originals untouched, count 3 -> 6. **`mirrorCopy = false` on CONNECTED MEP is the trap** — mirroring 3 ducts without their fittings gave -8462 / 2800 / -15225 instead, because Revit preserves the connections and re-fits rather than reflecting; 8 unconnected walls mirrored to the millimetre in the same run. No code bug — the header now carries the warning and the fix (include the fittings, or use copy mode) |
| [`action-flip-elements.cs`](actions/move-copy-rotate/action-flip-elements.cs) | Flip hand and/or facing orientation of FamilyInstances — Revit's own Flip arrows — ✓ verified 2026-08-07, **positive path finally proven and no longer blocked**: the fixture now contains a door (921817, `CanFlipHand` and `CanFlipFacing` both true), and a hand+facing run reported `Hand flipped on 1, facing flipped on 1, skipped 0` with `HandFlipped`/`FacingFlipped` genuinely going False -> True and back to False on rollback. Graceful path re-confirmed alongside it: 3 ducts (not FamilyInstances at all) -> skipped 3, no exception. The 2026-07-23 note that no flip-capable family was loaded is retired |
| [`action-offset-elements.cs`](actions/move-copy-rotate/action-offset-elements.cs) | Offset each linear element sideways by a perpendicular distance (mm) — copy or in-place — ✓ verified 2026-07-22, both modes exact |
| [`action-trim-extend-elements.cs`](actions/move-copy-rotate/action-trim-extend-elements.cs) | Trim or extend exactly 2 linear elements to meet at their computed corner — ✓ verified 2026-07-22 exact (coincident-endpoint caution in header) |
| [`action-fillet-elements.cs`](actions/move-copy-rotate/action-fillet-elements.cs) | Round the corner between exactly 2 elements — real elbow fitting for MEP curves, or a geometric tangent arc for Model/Detail lines — ✓ verified 2026-07-22 both modes (arc-mode bug found+fixed, story in header) |
| [`action-array-elements.cs`](actions/move-copy-rotate/action-array-elements.cs) | Multiple evenly-spaced copies — linear (fixed mm spacing) or radial (swept around a center) — ✓ verified 2026-07-22, both modes exact |
| [`action-move-to-ray-hit.cs`](actions/move-copy-rotate/action-move-to-ray-hit.cs) | Fire ONE ray per element and move it to whatever the ray hits, plus a signed offset — "terminal up to the slab", "sprinkler up to the soffit", "bracket across to the wall". Direction AND target category are per-request inputs; dry-run by default. The general form of `recipes/ray-trace-to-ceiling.cs` — ✓ verified 2026-07-26 (17 air terminals snapped to 3 ceilings at 3 different heights, each finding the one above itself, 0 misses) |
| [`action-align-elements.cs`](actions/move-copy-rotate/action-align-elements.cs) | Snap every element to match one reference element's X/Y/Z position — Revit's "Align" tool — ✓ verified 2026-07-22 exact |

**Structural Changes** — [`actions/structural-changes/`](actions/structural-changes/)
| Fragment | Job |
|---|---|
| [`action-change-element-type.cs`](actions/structural-changes/action-change-element-type.cs) | Bulk-swap every element's type to a different named type within the same family — ✓ verified 2026-08-07, positive and skip paths **and the difference between them matters here**: a first run against 'Generic - 150mm' reported `Changed 0, skipped 8` and that was CORRECT — that type does not exist in this model, so the run proved only the skip path. Re-run against 'Generic - 300mm', which does exist in the same Basic Wall family: `Changed 8, skipped 0`, every wall's type read back as 'Generic - 300mm' and its actual `Wall.Width` went 200 mm -> 300 mm. An immediate re-run reported `Changed 0, skipped 8` (already that type), so it is idempotent. A type name that matches nothing is skipped rather than throwing |
| [`action-delete-elements.cs`](actions/structural-changes/action-delete-elements.cs) | Permanently delete every element in the set — highest-risk fragment, explorer-first is mandatory, needs `allowDestructive: true` on the bridge call too — ✓ verified 2026-07-22 |
| [`action-group-elements.cs`](actions/structural-changes/action-group-elements.cs) | Bundle the filtered set into a new Model Group — ✓ verified 2026-08-07: grouped the 3 ducts into a named group (count 1 -> 2, and each duct's `GroupId` went -1 -> 922015, so membership is real and not just a new empty group). The `groupName` input renamed the GroupType as designed |
| [`action-ungroup-elements.cs`](actions/structural-changes/action-ungroup-elements.cs) | Dissolve Group instances in the set back into their members — paired undo for `action-group-elements.cs` — ✓ verified 2026-08-07 against a mixed list (1 real Group + 1 wall): reported `Ungrouped 1, skipped 1 (not a Group instance)`, the group count went 2 -> 1 and the ducts' `GroupId` went back to -1. Chained straight after `action-group-elements.cs`, so the pair proves each other |
| [`action-join-geometry.cs`](actions/structural-changes/action-join-geometry.cs) | Join (or unjoin) every element in the set with one target element — many-to-one — ✓ verified 2026-08-07, positive AND graceful paths. **Getting the positive path needed a real fixture check first**: an initial run reported `Joined 0, skipped 2` and that was correct — probing all 28 wall pairs showed only 20 can be joined, and the target first chosen happened to be one of the 8 that cannot. Against a joinable target it reported `Joined 2` with `AreElementsJoined` confirming True on both. Two gotchas worth knowing: `AreElementsJoined` reads stale straight after a join unless you `Document.Regenerate()` first, and joining is expensive enough that several join/unjoin cycles in one bridge call can hit the script time limit — when that happened the fragment's own catch rolled back and reported it, leaving the model clean, which is the behaviour it promises |
| [`action-split-elements.cs`](actions/structural-changes/action-split-elements.cs) | Split each Duct/Pipe at a point along its own length (fraction or mm from start), joint held by a real Union fitting (2026-07-26 fix: bare ConnectTo joints can be silently re-merged by Revit, losing the split) — generalized from `recipes/split-duct-near-equipment.cs` — ✓ verified 2026-07-22, union fix not yet live-run |
| [`action-purge-unused.cs`](actions/structural-changes/action-purge-unused.cs) | Delete unused View Templates, Filters, or Materials — the subset of native Purge Unused provably correct from the public API; does NOT consume `elements`, dry-run by default — ✓ dry-run 2026-07-22 all 3 modes; real delete not yet exercised |
| [`action-reload-links.cs`](actions/structural-changes/action-reload-links.cs) | Reload the distinct RVT link type(s) behind a set of link instances — Manage Links' Reload, scripted — enum flag RESOLVED 2026-08-04 (live, Revit 2020): `LinkNotNeeded` was NOT a real member and the fragment could never compile; now `UsedExisting`, graceful path ✓ live. Positive path still BLOCKED (0 links) |
| [`action-unload-remove-links.cs`](actions/structural-changes/action-unload-remove-links.cs) | Unload (keep, drop from memory) or REMOVE (delete from project — destructive, `allowDestructive: true` required) the link type(s) behind a set of link instances — completes Manage Links alongside `action-reload-links.cs` — BLOCKED (0 links), graceful path only (2026-07-26 gap backlog) |
| [`action-add-remove-insulation.cs`](actions/structural-changes/action-add-remove-insulation.cs) | Add or remove insulation (ducts+pipes) or lining (ducts only) — the WRITE counterpart to the two insulation filters; one insulation per element, already-insulated skipped+reported — ✓ verified live 2026-08-14 on a scratch model, **all three branches**: add put 25 mm on 5 of 5 ducts (read back as 5 real insulation elements), re-add correctly added 0 and skipped 5, remove deleted all 5 leaving the ducts intact. The old BLOCKED note is cleared — the fixture was built with `Duct.Create`, it never needed to be found |
| [`action-extract-cad-curves.cs`](actions/structural-changes/action-extract-cad-curves.cs) | Trace linked/imported CAD into Revit Model/Detail lines, filtered by DWG layer — dry-run first reports curve counts PER LAYER so the layer filter comes from reality; polylines exploded, splines skipped+counted — NOT yet live-verified, needs a CAD fixture (2026-07-26 round 3, Bimorph-equivalent) |
| [`action-place-accessory-on-run.cs`](actions/structural-changes/action-place-accessory-on-run.cs) | Insert a duct/pipe ACCESSORY (VCD, damper, valve) INTO each run; produces `newAccessoryIds` — MODIFIES existing runs, explorer-first applies. **REWRITTEN 2026-08-14 after the first live run DISPROVED the original**, which assumed `NewFamilyInstance` with the run as host breaks it automatically the way the UI does — it does not: 0/2 connectors joined, null host, and it reported success. Three causes found live, each of which alone looks like the whole answer: (1) the overload never breaks the run, so `BreakCurve` must be called explicitly; (2) a new accessory takes its FAMILY DEFAULT size, not the run's, and a 300 accessory on a 200 duct cannot connect; (3) **joining both halves inside one transaction raises a modal "cannot keep the connectivity" dialog at commit, which a bridge script cannot answer — the call HANGS rather than fails.** ✓ the METHOD is proven live (2/2 joined, both halves 1/1); ✗ the rewritten file as one uninterrupted run is NOT yet verified — read its STATUS block before use, and run it on one run at a time |
| [`action-purge-unused-families.cs`](actions/structural-changes/action-purge-unused-families.cs) | Find/delete loadable family TYPES with zero placed instances, and whole families where every type is unused — the file-size half `action-purge-unused.cs` leaves out; nested symbols protected; dry-run by default, `allowDestructive: true` to really delete — ✓ dry-run verified 2026-07-26 (found 184/184 types and 107 whole families unused); real delete unexercised |
| [`action-copy-from-link.cs`](actions/structural-changes/action-copy-from-link.cs) | Copy elements FROM a linked model INTO the host at true position (link transform applied) — source Ids are LINKED-doc Ids from `filter-by-linked-model-elements.cs`; copies never update with the link (say so every time) — BLOCKED (0 links), NOT yet live-verified (2026-07-26 round 3) |
| [`action-duplicate-type.cs`](actions/structural-changes/action-duplicate-type.cs) | Duplicate the distinct TYPE(s) behind a set of instances under a new name (prefix/suffix/fixed) — Type-level counterpart to `action-rename-family.cs` — ✓ verified 2026-07-22 |
| [`action-update-scope-box.cs`](actions/structural-changes/action-update-scope-box.cs) | Report a named Scope Box's dependent views (does NOT consume `elements`) — resize CONFIRMED IMPOSSIBLE on Revit 2020 (needs Scope Box creation, which has no API — see create-scope-box.cs); the destructive resize step was removed entirely |

**Sheets & Views** — [`actions/sheets-views/`](actions/sheets-views/)
| Fragment | Job |
|---|---|
| [`action-place-viewport-on-sheet.cs`](actions/sheets-views/action-place-viewport-on-sheet.cs) | Place each view in `elements` onto one target sheet as a Viewport — ✓ verified 2026-08-07 and **fixed live**. The fixture was built with the already-verified `create-sheet.cs` inside the same rolled-back group (the model has 0 sheets). **The first pass recorded only "Placed 2 viewport(s)" and that was not enough** — an adversarial re-read flagged that the default point was computed inside the loop with no per-element offset, and a 3-view run confirmed it: 3 viewports, ONE distinct centre (422.5, 296.0)mm, perfectly stacked, reported as a clean success on a sheet that was unusable. It now lays viewports out on a grid and prints each position. Re-proved: 4 views -> 2x2, 4 distinct centres; 1 view still lands mid-sheet at (422, 296) exactly as before; an explicit `pointXmm`/`pointYmm` still stacks them (the caller's choice) but now says so. The `Viewport.CanAddViewToSheet` guard skips a non-placeable input rather than throwing |
| [`action-place-schedule-on-sheet.cs`](actions/sheets-views/action-place-schedule-on-sheet.cs) | Place each schedule onto one sheet — same schedule can be placed on multiple sheets, no duplication needed — ✓ verified 2026-08-07. The model has 0 sheets, so the fixture was BUILT with the already-verified `create-sheet.cs` inside the same TransactionGroup and rolled back with everything else — the create-then-rollback technique, which makes a permanent fixture unnecessary. It reported `Placed 1 schedule(s) ... skipped 2` from what looked like a 2-item list, and chasing that discrepancy proved the fragment RIGHT: **creating a sheet with a title block silently adds a `<Revision Schedule>` ViewSchedule** (`IsTitleblockRevisionSchedule = True`) plus one auto ScheduleSheetInstance, so the real list was 3 and the fragment's `IsTitleblockRevisionSchedule` guard correctly refused to place it. Placing a revision schedule as a normal schedule would have been the bug. ScheduleSheetInstances went 1 (auto) -> 2 (ours). **Known, NOT fixed**: like its viewport sibling it computes one top-left point for every schedule, so placing several stacks them — harmless with the single schedule this model has, real with more |
| [`action-duplicate-views.cs`](actions/sheets-views/action-duplicate-views.cs) | Duplicate/duplicate-with-detailing/dependent-view each view in the set; produces `newViewIds` — ✓ verified 2026-08-07, **all three duplicate options**, each against the document's real view count: `WithDetailing` and `AsDependent` duplicated the floor plan only (19 -> 20), while `Duplicate` also took the schedule (19 -> 21) — a schedule can be plain-duplicated but not detailed or made dependent, and `CanViewBeDuplicated` is what makes that difference show up as a `skipped` rather than a throw. A duct passed in alongside was skipped as not-a-View |
| [`action-apply-view-template.cs`](actions/sheets-views/action-apply-view-template.cs) | Apply an existing View Template to one or more views — does NOT consume `elements` — ✓ verified 2026-08-07: applying 'Mechanical Plan' to the active view moved its `ViewTemplateId` from -1 to 142574 (confirmed by re-reading the view, not from the message). Both guard paths fired too: a name that does not exist reports the miss and lists the 16 available templates, and pointing it at a View Template itself reports `Skipped: Architectural Plan (is itself a template)` rather than nesting a template inside a template |
| [`action-create-view-template-from-view.cs`](actions/sheets-views/action-create-view-template-from-view.cs) | Save a configured view's current settings as a new named View Template — does NOT consume `elements` — ✓ verified 2026-07-22 (`CreateViewTemplate()` returns a `View`, not an `ElementId`) |
| [`action-set-view-template-controlled-params.cs`](actions/sheets-views/action-set-view-template-controlled-params.cs) | Include/Exclude which parameters a View Template controls on one view — does NOT consume `elements` — ✓ verified 2026-07-22 |
| [`action-remove-view-template.cs`](actions/sheets-views/action-remove-view-template.cs) | Detach a View Template from one or more views, optionally delete it from the document entirely — the paired undo for `action-apply-view-template.cs` — does NOT consume `elements` — ✓ verified 2026-08-07 (non-destructive path) against a view that genuinely had one, chained after `action-apply-view-template.cs`: `ViewTemplateId` went 142574 -> -1, and re-running reported `Skipped: 1 - Mech (no template applied)` instead of miscounting. All 16 template definitions survived. The `deleteTemplateElement = true` path stays unrun — like `action-remove-view-filter.cs` it needs `allowDestructive: true` merely because `Document.Delete` appears in the text |
| [`action-duplicate-view-template.cs`](actions/sheets-views/action-duplicate-view-template.cs) | Duplicate an existing View Template (by name) into a new, separately-named template — does NOT consume `elements` — ✓ verified 2026-08-07 and **rewritten, because it could never have worked**: it called `View.Duplicate` behind a `CanViewBeDuplicated` guard that returns FALSE for every View Template on all three options, so it always reported "cannot be duplicated" and did nothing. The guard was not the bug — bypassing it throws `View cannot be duplicated`; a template simply cannot be copied that way on Revit 2020. **[`../knowledge/live-model/views.md`](../knowledge/live-model/views.md) had already recorded the working technique on 2026-08-01** (`ElementTransformUtils.CopyElements`, plus the fact that Revit auto-suffixes the copy so the rename must be a second transaction) — this fragment now uses it. Verified: 'Mechanical Plan' (scale 100, detail Medium) copied to a template with the same values, count 16 -> 17, source unchanged, no existing view touched; both guard paths (name not found, target name already taken) also fired |
| [`action-report-view-template-status.cs`](actions/sheets-views/action-report-view-template-status.cs) | Report whether one or more views have a View Template applied — which one, and which parameters are excluded from its control; read-only — does NOT consume `elements` — ✓ verified 2026-08-07, **all three modes and the zero proven**: active-view-only, `checkAllViews` (walked all 18 real views, correctly excluding schedules and templates), and targeting a View Template itself gave `is itself a View Template, not a real view`. The summary said `0 with a template applied` — which is the model's default — so a template was applied inside a transaction and the count went to 1, then back to 0 on rollback |
| [`action-add-schedule-field.cs`](actions/sheets-views/action-add-schedule-field.cs) | Add one or more columns/fields to existing schedules — same call `create-schedule.cs` uses, for a schedule that already exists — ✓ verified 2026-07-22 |
| [`action-remove-schedule-field.cs`](actions/sheets-views/action-remove-schedule-field.cs) | Remove one or more columns/fields by name — paired undo for `action-add-schedule-field.cs` — ✓ verified 2026-07-22 |
| [`action-set-schedule-field-format.cs`](actions/sheets-views/action-set-schedule-field-format.cs) | Format one existing column — heading override, hide/show, alignment, sheet column width — ✓ verified 2026-07-22 |
| [`action-set-schedule-filters.cs`](actions/sheets-views/action-set-schedule-filters.cs) | Replace ALL filter rules on a schedule (field/type/value list) — clears old filters first, not additive — ✓ verified 2026-07-22 |
| [`action-set-schedule-sort-group.cs`](actions/sheets-views/action-set-schedule-sort-group.cs) | Replace ALL sort/group fields on a schedule, in order — clears old sort/group first, not additive — ✓ verified 2026-07-22 |
| [`action-set-schedule-appearance.cs`](actions/sheets-views/action-set-schedule-appearance.cs) | Set "Itemize every instance" and Grand Total row — the 2 appearance settings with solid API confidence; the rest of the Appearance tab deliberately NOT covered — ✓ verified 2026-07-22 (`ShowGrandTotal` is a plain `bool` on 2020) |
| [`action-report-schedule-fields.cs`](actions/sheets-views/action-report-schedule-fields.cs) | List every field/column on a schedule IN ORDER — name, heading, type, hidden, alignment; read-only — ✓ verified 2026-08-07 against the real 'Space Outdoor Air Schedule': listed all 9 fields **in schedule order** with their overridden column headings intact (`Outdoor Air per Area` heading `Outdoor Air per Area (Ra)`), and correctly distinguished field types — 8 `Instance` and 1 `ViewBased` (Area). A duct passed in alongside was reported as `1 non-ViewSchedule element(s) skipped` |
| [`action-report-schedule-definition.cs`](actions/sheets-views/action-report-schedule-definition.cs) | Read back HOW a schedule is built, not its data — category (incl. `<Multi-Category>`), filter rules with category/element IDs resolved to names, sort/group levels in order, the itemised/headers/grand-total/links switches, and per column whether its value is a built-in parameter (named), a SHARED parameter (with GUID), a project parameter, or a schedule formula; read-only — ✓ verified 2026-08-19 against the real multi-category 'MM_V03' on 4355-BHVD-3D-60P00-BL006A: resolved all 8 `Category NotEqual` rules to names (Pipes, Duct Fittings, Cable Trays…), the 4-level sort, and 13 shared-parameter GUIDs. Formula TEXT is unreadable on Revit 2020 — `ScheduleField` has no formula member at all, so it is detected by reflection and reported as UI-only |
| [`action-add-schedule-calculated-field.cs`](actions/sheets-views/action-add-schedule-calculated-field.cs) | Add a Combined Parameter field to a schedule — ✓ verified 2026-07-22, Revit-2020 API (`TableCellCombinedParameterData` — story in header); mode="formula" is a confirmed 2020 gap (UI-only), reports instead of throwing |
| [`action-export-sheets-to-pdf.cs`](actions/sheets-views/action-export-sheets-to-pdf.cs) | Batch-export ViewSheets to PDF via `Document.PrintManager` (Revit 2020 has no PDF export API — story in header) with a safety guard against this system's physical printer — reflection-verified, NOT live-executed; needs a user go-ahead for the first real print |
| [`action-set-view-properties.cs`](actions/sheets-views/action-set-view-properties.cs) | Batch-set Scale, Detail Level, Visual Style, and/or Phase/Phase Filter across views — the lightweight direct version of applying a View Template — ✓ verified 2026-07-22 |
| [`action-tag-elements.cs`](actions/sheets-views/action-tag-elements.cs) | Simple tag placement (fixed offset, optional leader) in one view — reuses the proven `IndependentTag.Create` pattern from `recipes/tag-elements-in-active-view.cs` without its clash-scoring — ✓ verified 2026-07-22 |
| [`action-remove-tags.cs`](actions/sheets-views/action-remove-tags.cs) | Delete IndependentTag elements in the set — paired undo for `action-tag-elements.cs` — ✓ verified 2026-08-07. The model had 0 tags, so 3 were placed first with the already-verified `action-tag-elements.cs` inside the same rolled-back group. Reported `Removed 3 tag(s), skipped 1` and the view's tag count went 3 -> 0. **The safety check that matters passed**: a duct was deliberately included in the input and the `is IndependentTag` guard skipped it — duct count stayed 3, so the fragment cannot be tricked into deleting model elements alongside their tags. Needs `allowDestructive: true` (it calls `Document.Delete`) |
| [`action-export-schedule-to-csv.cs`](actions/sheets-views/action-export-schedule-to-csv.cs) | Export ViewSchedules to CSV via Revit's native `ViewSchedule.Export` — one file per schedule — ✓ verified 2026-07-22 (real CSV content confirmed) |
| [`action-report-sheet-title-blocks.cs`](actions/sheets-views/action-report-sheet-title-blocks.cs) | Report which title block (Family + Type) is on each sheet — read-only — ✓ verified 2026-08-07. The model has 0 sheets, so the fixture was BUILT with the already-verified `create-sheet.cs` inside the same TransactionGroup and rolled back with everything else — the create-then-rollback technique, which makes a permanent fixture unnecessary. Reported `1 sheet(s), 0 with NO title block placed` and resolved the instance correctly — `A-101 | New Sheet | A1 metric | A1 metric | 922032` — matching the title block Revit auto-placed with the sheet. The `(none)` branch for a sheet without a title block is the one path still unexercised |
| [`action-set-sheet-title-block.cs`](actions/sheets-views/action-set-sheet-title-block.cs) | Change each sheet's title block to a named Type — in-place `ChangeTypeId` if same family, delete+replace at origin if a different family — ✓ verified 2026-07-22 |
| [`action-manage-sheet-sets.cs`](actions/sheets-views/action-manage-sheet-sets.cs) | Named Sheet/View Sets (`ViewSheetSet`, the Print/Export saved sets) — report / create-from-`elements` / delete / rename; pairs with `action-export-sheets-to-pdf.cs` — ✓ ALL FOUR MODES verified live 2026-08-14 on sheets M-901/902/903, each read back from a separate call. FIXED same day: rename/delete went through `Element.Name`/`Document.Delete` and rename FAILED live ("set the name via PrintSetup::Rename"); both now use `ViewSheetSetting.Rename()`/`.Delete()` after assigning `CurrentViewSheetSet` — which the old header wrongly called unviable |
| [`action-export-views-to-dwg.cs`](actions/sheets-views/action-export-views-to-dwg.cs) | Export each View/Sheet in `elements` to its own DWG (optional saved export setup by name) — writes files on disk, go-ahead first — NOT yet live-verified (2026-07-26 gap backlog) |
| [`action-export-ifc.cs`](actions/sheets-views/action-export-ifc.cs) | Export the model (or one 3D view's scope) to IFC — IFC2x3CV2/IFC4/IFC2x3; does NOT consume `elements`; transaction-wrapped on purpose (FLAGGED note in header) — NOT yet live-verified (2026-07-26 gap backlog) |
| [`action-export-nwc.cs`](actions/sheets-views/action-export-nwc.cs) | Export the model (or one 3D view's scope) to Navisworks NWC — detects the exporter add-in first, reports gracefully if absent; does NOT consume `elements` — NOT yet live-verified (2026-07-26 gap backlog) |
| [`action-export-view-image.cs`](actions/sheets-views/action-export-view-image.cs) | Export each View/Sheet in `elements` as a PNG at a chosen pixel width — the "screenshot this view properly" job — NOT yet live-verified (2026-07-26 gap backlog) |
| [`action-add-spot-elevations.cs`](actions/sheets-views/action-add-spot-elevations.cs) | Place a Spot Elevation on each element in one view — reference dug from element geometry; no-reference elements skipped+reported; new `facePreference` input ("top"/"bottom"/"any") and the annotated level is now printed per element — ✓ verified live 2026-08-14 on a 300mm floor. **SILENT-SUCCESS BUG FOUND AND FIXED** the same day: "first planar face with a Reference" picked the SOFFIT and annotated -300 mm while reporting "1 placed, 0 skipped". Revit returns the bottom face first. The guard must be `FaceNormal.Z > 0.9` — `Math.Abs(...) > 0.9` does NOT catch it |
| [`action-set-print-settings.cs`](actions/sheets-views/action-set-print-settings.cs) | Report the current driver's paper sizes + saved Print Settings, or save a named Print Setting (paper/orientation/zoom) — completes the print chain with `action-manage-sheet-sets.cs` + PDF export; does NOT consume `elements` — ✓ report mode verified 2026-07-26 (34 paper sizes off a PHYSICAL Kyocera 6008ci); save mode still unexercised |
| [`action-duplicate-sheet.cs`](actions/sheets-views/action-duplicate-sheet.cs) | Duplicate each sheet — same title block, views duplicated and placed at the SAME viewport positions, schedules re-placed; produces `newSheetIds`; loose sheet annotations/guide grids/revisions NOT copied (header) — ✓ verified 2026-08-07 and **fixed live — the header was making a false promise**. It claimed a number collision "fails that one sheet cleanly (reported), the rest continue". It did not: the sheet is created BEFORE its number is assigned, so a collision threw after creation and the per-sheet catch merely counted it failed while the transaction committed. Measured — duplicating 'A-101' into an already-taken 'A-101-COPY' reported `Duplicated 0 of 1 sheet(s), 1 failed` while the sheet count went **2 -> 3**, leaving an orphan 'A-103' carrying Revit's own auto-number. The catch now deletes the part-built sheet and says so. Re-proved both ways: the colliding run fails with `(the part-built sheet was removed)` and the count stays at 2; a free suffix creates 'A-101-B' and carries the source viewport across (1 placed, 1 skipped — the skipped one being the title block's auto revision schedule). Needs `allowDestructive: true`, only to remove its own failed sheet |
| [`action-add-aligned-dimensions.cs`](actions/sheets-views/action-add-aligned-dimensions.cs) | One aligned dimension string through 2+ FamilyInstances along X or Y — uses the families' own centre references so the dimension holds when elements move; sorted into drafting order; reference-less families skipped+reported — extends `create-dimension.cs` past Grid/Level — ✓ verified live 2026-08-14: 3 diffusers at 2500 mm centres produced a 2-segment dimension reading 2500.0 mm each, read back from a separate call. FamilyInstance-only (walls/floors/ducts are skipped). Measured: stock air terminals expose CenterLeftRight+CenterFrontBack, but **duct fittings and duct accessories expose NO references at all** — "no usable reference" there is expected, not a bug |

**Sheet Dates & Revisions** — [`actions/sheet-dates-revisions/`](actions/sheet-dates-revisions/)
| Fragment | Job |
|---|---|
| [`action-extract-dates-from-textnotes.cs`](actions/sheet-dates-revisions/action-extract-dates-from-textnotes.cs) | Scan every TextNote on each sheet for date-like text, report distinct dates + source sheet(s), read-only — ✓ verified 2026-08-07, **and the two things that could go wrong both went right**: a deliberately invalid '32-Aug-2026' was REJECTED by the `new DateTime(...)` guard rather than parsed into nonsense, and the same date written two ways ('07-Aug-2026' and '07/Aug/2026' on different sheets) folded to ONE key listing both sheets. Read-only, nothing touched. Verified 2026-08-07 as one lifecycle on a purpose-built fixture (3 sheets, dated text notes, a scoped revision), all rolled back. |
| [`action-assign-revisions-by-sheet-date.cs`](actions/sheet-dates-revisions/action-assign-revisions-by-sheet-date.cs) | Attach each sheet's matching project Revision(s) via `SetAdditionalRevisionIds`, matched by date found in that sheet's TextNotes — writes the model, see gotcha note in `../knowledge/live-model/revisions.md` — ✓ verified 2026-08-07: the scope filter (`onlyDescription`/`onlyIssuedTo`/`onlyIssuedBy`) narrowed 2 project revisions to the 1 intended, so an unrelated revision sharing a date could not be attached by accident — which is exactly the risk the INPUTS comment warns about. Sheets M-101 and M-102 got the revision, M-103 (no date in its note) was reported as skipped, and the counts were confirmed by a **post-commit read-back**, which is the header's own standing rule. Verified 2026-08-07 as one lifecycle on a purpose-built fixture (3 sheets, dated text notes, a scoped revision), all rolled back. |
| [`action-remove-revision-from-sheet.cs`](actions/sheet-dates-revisions/action-remove-revision-from-sheet.cs) | Detach named Revision(s) from each sheet, matched by Description — the reverse of `action-assign-revisions-by-sheet-date.cs` — ✓ verified 2026-08-07 against sheets that genuinely had a revision attached (chained straight after `action-assign-revisions-by-sheet-date.cs`, so the pair prove each other): matched by Description, removed 1 from each of the two sheets and reported them by name, leaving the third — which never had one — untouched and unmentioned. Verified 2026-08-07 as one lifecycle on a purpose-built fixture (3 sheets, dated text notes, a scoped revision), all rolled back. |
| [`action-report-revisions.cs`](actions/sheet-dates-revisions/action-report-revisions.cs) | List every project Revision in order — Seq/Date/Description/Issued By/To/Issued/Visibility — does NOT consume `elements` — ✓ verified 2026-08-07: listed the model's 1 revision with every column populated — `Seq 1 | Date 1 | Revision 1 | | | False | CloudAndTagVisible` — including the `Issued` flag and `Visibility` enum that `action-edit-revision.cs` needs to target |
| [`action-edit-revision.cs`](actions/sheet-dates-revisions/action-edit-revision.cs) | Update an existing Revision's fields (description/date/issued by/to/issued flag/visibility), matched by SequenceNumber — does NOT consume `elements` — ✓ verified 2026-08-07, every branch: all five fields written and read back (Date, Description, IssuedBy, IssuedTo, Visibility -> TagVisible); all-inputs-null correctly reports "updated nothing — no fields set"; an unknown SequenceNumber lists what IS available. **The per-field try/catch earns its keep**: with the revision already Issued, a later Description edit failed with Revit's "This operation cannot be performed on Revisions that have already been issued" and was reported under `Failed fields:` instead of taking the whole run down. A suspected ordering hazard — `Issued` being set before `Visibility` in the same run — did NOT reproduce: the issued-lock only bites in a SUBSEQUENT transaction, so both landed |
| [`action-delete-revision.cs`](actions/sheet-dates-revisions/action-delete-revision.cs) | Permanently delete Revision(s) by SequenceNumber — completes the Create/Edit/Delete lifecycle alongside `creators/create-revision.cs`; renumbers later revisions — does NOT consume `elements` — ✓ verified 2026-07-22 (deletion + renumbering confirmed) |

### Creators (produce `elements` by creating new ones)
| Fragment | Job |
|---|---|
| [`create-levels.cs`](creators/create-levels.cs) | Batch-create levels, evenly spaced or at explicit elevations — ✓ verified 2026-08-07 and **fixed live**: `.ToHashSet()` used the default ORDINAL comparer, so a lowercase `namePrefix` did not see the existing 'Level 1' and named a new level **'level 1'** — and Revit accepts that, leaving two levels differing only in case. Now uses `OrdinalIgnoreCase`, matching `create-material.cs` which already solved the same problem. Re-proved: prefix 'Level ' -> Level 3 / Level 4, prefix 'level ' -> 'level 3' rather than 'level 1'. Its summary also echoed the REQUESTED elevation list with no names and no Ids — it now prints each level's name, Id and elevation read back off the Level, per this README's own report-the-Element-ID rule. Elevations landed exactly (9000, 12000, 15000 mm) |
| [`create-material.cs`](creators/create-material.cs) | Create one or more Materials with a set colour and transparency — ✓ verified 2026-08-07, every branch in one run of 4 specs -> `Created 2, skipped 2`: colour and transparency landed exactly (RGB 12,34,56 at 40%), an out-of-range transparency of 250 **clamped to 100**, a name already in the document was skipped, and a name repeated **within the same batch** was also skipped because `existingNames` is added to inside the loop. The important safety check: the existing 'Default Wall' material kept its own colour (128,128,128) rather than being overwritten with the requested (1,2,3) — the skip is a real skip, not a silent edit. Count 70 -> 72 |
| [`create-point-based-element.cs`](creators/create-point-based-element.cs) | Place a family instance at one or more points on a level — ✓ verified 2026-08-07: placed 3 instances of an **inactive** symbol ('M_Exhaust Grill', one of 73 inactive FamilySymbols in this model) at exactly the requested mm points including a non-zero Z, air terminal count 5 -> 8, and the no-match input reported "No FamilySymbol found matching ..." rather than placing something arbitrary. **A flagged concern did NOT reproduce**: the fragment calls `symbol.Activate()` with no following `Document.Regenerate()`, and `load-family.cs`'s header asserts this file handles that — `NewFamilyInstance` succeeded anyway on this API, so the claim holds in outcome even though the call is absent. Recorded rather than 'fixed', since inventing a fix for a failure that does not occur would be guessing |
| [`create-room.cs`](creators/create-room.cs) | Place a Room at one or more points on a level — ✓ verified 2026-08-07 and **fixed live**: `room.get_Parameter(...)?.Set(name)` swallowed two failures at once — the null-conditional made "no such parameter" identical to "written", and the discarded bool hid a refusal. It now checks both and names the room Ids. **The predicted duplicate-Number refusal did NOT reproduce**: Revit ALLOWED a second room numbered 'T01' beside the existing one (both unbounded), so the refusal is conditional rather than guaranteed — which is precisely why it must be checked rather than assumed. The header's own "check `room.Area > 0`" advice works: both test rooms were flagged `came out unbounded (zero area)` |
| [`create-rooms-in-enclosed-regions.cs`](creators/create-rooms-in-enclosed-regions.cs) | Fill EVERY enclosed region on a level with a Room in one pass, reusing the project's existing UNPLACED rooms first so you don't orphan them — no coordinates needed (asks Revit's `PlanTopology` which regions are closed). The unblocker for room-based routing/coverage/filters: an unplaced room (Area 0) makes all of them silently return nothing — ✓ verified 2026-07-26 (3 regions, 3 unplaced rooms reused: 238.6/251.0/270.8 m²) |
| [`create-space.cs`](creators/create-space.cs) | Place an MEP Space at one or more points on a level — Space-category equivalent of create-room.cs — ✓ verified 2026-07-22 (Name/Number GOTCHA — see filter-by-space.cs) |
| [`create-sheet.cs`](creators/create-sheet.cs) | Create one or more new sheets with a chosen title block — explicit (number, name) list, OR a SEQUENCE run (prefix + running number + padding, names counting 01, 02, 03...) for a drawing series; existing sheet numbers skipped, not overwritten — ✓ verified 2026-07-22, sequence mode ✓ verified 2026-07-27 (26 HVAC layout sheets) |
| [`create-schedule.cs`](creators/create-schedule.cs) | Create a bare schedule for a category with chosen fields — chain into `action-place-schedule-on-sheet.cs` — ✓ verified 2026-07-22 |
| [`create-text-note.cs`](creators/create-text-note.cs) | Place one or more Text Notes at given points in a view — ✓ verified 2026-08-07: created 2 notes in the active view at exactly the requested mm coordinates — (0,0) and (2500,1500), read back off `TextNote.Coord` — with the right text and `OwnerViewId` 312. The `textNoteTypeName` miss path reports "No Text Note type found named ..." rather than falling back to an arbitrary type |
| [`create-dimension.cs`](creators/create-dimension.cs) | Create a dimension string across 2+ Grids/Levels — deliberately scoped to Grid/Level references only, not arbitrary element geometry — ✓ verified 2026-07-22 exact |
| [`create-line.cs`](creators/create-line.cs) | Create one or more plain Model Lines or Detail Lines between mm point pairs — ✓ verified 2026-07-22 |
| [`create-filled-region.cs`](creators/create-filled-region.cs) | Create a filled/hatched polygon annotation from a closed loop of mm points — ✓ verified 2026-07-22 |
| [`create-grid.cs`](creators/create-grid.cs) | Create one or more straight Grids from mm endpoint pairs — ✓ verified 2026-08-07 with all three branches in one run: from 3 requested grids it reported `Created 2` — the **degenerate entry (identical start and end point) was skipped by the length guard**, not turned into a crash. 'ZA' took its requested name; '1' **collided with an existing grid** and, exactly as the inline comment promises, the grid was still created and simply kept Revit's default name ('6'). Count 4 -> 6 |
| [`create-view.cs`](creators/create-view.cs) | Create a Floor Plan, 3D, or Section view — the three simple/reliable ViewFamily cases, not Callout/Elevation/Drafting — ✓ verified 2026-08-07 (all four branches) and **section mode fixed live — it was producing the wrong kind of view entirely**. `BoundingBoxXYZ` starts with an IDENTITY Transform and `CreateSection` reads the look direction from its `BasisZ`, so the section came out with `ViewDirection (0,0,-1)` — looking straight DOWN, the same axis as a floor plan — and still reported "Created section view". [`../knowledge/live-model/views.md`](../knowledge/live-model/views.md) already specified the right-handed basis this needs; it simply was not being built. Now constructs BasisX (in-view left-right) / BasisY (global Z, up) / BasisZ = X cross Y, runs the section along the box's longer plan side, and expresses the extents in the section's own coordinates. Re-proved: a box long in Y looks (-1,0,0), long in X looks (0,1,0), `right x up` equals `ViewDirection` in both, and a cut through the real building shows 9 elements rather than an empty slice. It now also prints the direction it actually looks and warns if a 'section' comes out looking up/down. The catch prints `ex.GetType().Name` too, because views.md records that this call's exception Message comes back EMPTY. `floor_plan` -> (0,0,1), `three_d` -> isometric, unknown kind -> the usage message |
| [`create-room-elevations.cs`](creators/create-room-elevations.cs) | Place an `ElevationMarker` at a room's center (or an mm point) in a plan view and create 1-4 interior elevation views — fills the Elevation slot `create-view.cs` excludes; marker not auto-rotated to walls (gotcha in header) — ✓ **verified live 2026-08-14**: on a 43.2 m² room built by API, the marker landed on the room's true centre (24000, 3000) and all 4 slots produced views, renamed by `namePrefix`, scale 1:50, none a template. Confirmed independently — ViewSection count 16 → 20 — not from the script's own report |
| [`create-floor.cs`](creators/create-floor.cs) | Create one flat Floor from a closed mm boundary on a Level — legacy `NewFloor` (static `Floor.Create` is 2022+, don't "modernize") — ✓ verified 2026-08-07: created a 4000x3000 mm floor whose computed area read back as exactly **12.00 m2**, on Level 1, type resolved to the first FloorType ('Generic 300mm'), count 0 -> 1, rolled back. The <3-points guard and the missing-FloorType message are the two paths not exercised (this project has FloorTypes) |
| [`create-revision.cs`](creators/create-revision.cs) | Create one or more Revisions directly (date/description/issued-to/by already known) — plain version of `recipes/create-revisions-from-sheet-dates.cs` — ✓ verified 2026-07-22 |
| [`create-workset.cs`](creators/create-workset.cs) | Create one or more new user Worksets — feeds `action-set-workset.cs`; produces no `elements` (Workset isn't an Element) — BLOCKED (model isn't workshared); graceful path ✓ |
| [`create-scope-box.cs`](creators/create-scope-box.cs) | CONFIRMED IMPOSSIBLE on Revit 2020 — no Scope Box creation API exists (reflection-confirmed; UI-only, View tab > Scope Box) — fragment reports this instead of a compile error |
| [`load-family.cs`](creators/load-family.cs) | Load .rfa files from disk into the project (File > Load Family) — produces the loaded FamilySymbols; deliberately does NOT overwrite an existing same-name family — ✓ verified 2026-08-14: loaded M_Electrical Panel (3 types) + M_Outlet-Duplex (1 type) from the stock US Metric library in one call, and the not-found path rolled back cleanly. **It never needed Ajmal to supply an .rfa** — `C:\ProgramData\Autodesk\RVT 2020\Libraries\US Metric\` ships thousands. **Its documented "KNOWN BUG" is not real**: already-loaded returns ok=False AND fam=NULL, so it lands in the reported branch. A catch-all `else` was added anyway, and the header now says so |
| [`create-duct.cs`](creators/create-duct.cs) | Draw ONE straight duct between two mm points (system type, duct type, level, W/H or dia) — the plain version of what the HVAC recipes do; unconnected segment, check open ends after — ✓ verified 2026-08-07. **Fixed live 2026-08-07 across all four MEP creators**: an MEP size can be accepted AND changed — `Set()` returns true and Revit snaps the value to the type's size table. A pipe asked for 77 mm returned true and came out **80 mm**. These fragments printed the REQUESTED numbers, so they would have reported a 77 mm pipe that does not exist. They now report ACTUAL size, re-read from the model, and flag REFUSED or SNAPPED explicitly. The read-back must follow `Document.Regenerate()` — straight after `Set()` the parameter still echoes the request. See [`../knowledge/live-model/core.md`](../knowledge/live-model/core.md). Proved on this model: asked 337x250 and got exactly 337x250 (a duct type accepts arbitrary sizes), duct count 3 -> 4, rolled back. `Set(0)` on Width is the refusal case and returns false |
| [`create-pipe.cs`](creators/create-pipe.cs) | Draw ONE straight pipe between two mm points — Plumbing twin of `create-duct.cs`; diameter snaps to the type's allowed sizes — ✓ verified 2026-08-07, and **this is the fragment that exposed the snap**. **Fixed live 2026-08-07 across all four MEP creators**: an MEP size can be accepted AND changed — `Set()` returns true and Revit snaps the value to the type's size table. A pipe asked for 77 mm returned true and came out **80 mm**. These fragments printed the REQUESTED numbers, so they would have reported a 77 mm pipe that does not exist. They now report ACTUAL size, re-read from the model, and flag REFUSED or SNAPPED explicitly. The read-back must follow `Document.Regenerate()` — straight after `Set()` the parameter still echoes the request. See [`../knowledge/live-model/core.md`](../knowledge/live-model/core.md). Re-proved both ways: asked 77 mm -> reports `ACTUAL dia 80 mm` plus `Diameter SNAPPED 77mm -> 80mm (nearest size 'Standard' allows)`; asked 50 mm -> reports 50 mm with no note, so the flag is not noise. Its own header had warned "read the value back if the exact size matters" and the code did not |
| [`create-cable-tray.cs`](creators/create-cable-tray.cs) | Draw ONE straight cable tray between two mm points (type, level, W/H) — ✓ verified 2026-08-07. **It was the worst of the four**: no else branch at all, so a missing parameter, a read-only one and a refused value were identical silence, and the summary printed `{widthMm}x{heightMm}` straight from the input. **Fixed live 2026-08-07 across all four MEP creators**: an MEP size can be accepted AND changed — `Set()` returns true and Revit snaps the value to the type's size table. A pipe asked for 77 mm returned true and came out **80 mm**. These fragments printed the REQUESTED numbers, so they would have reported a 77 mm pipe that does not exist. They now report ACTUAL size, re-read from the model, and flag REFUSED or SNAPPED explicitly. The read-back must follow `Document.Regenerate()` — straight after `Set()` the parameter still echoes the request. See [`../knowledge/live-model/core.md`](../knowledge/live-model/core.md). Proved: asked 337x100 on the 'Default' tray type (default 305x76) and got exactly 337x100 |
| [`create-conduit.cs`](creators/create-conduit.cs) | Draw ONE straight conduit between two mm points (type, level, dia) — ✓ verified 2026-08-07. Its summary printed `dia {diameterMm} mm` from the input while its own header already warned the size snaps to the type's table. **Fixed live 2026-08-07 across all four MEP creators**: an MEP size can be accepted AND changed — `Set()` returns true and Revit snaps the value to the type's size table. A pipe asked for 77 mm returned true and came out **80 mm**. These fragments printed the REQUESTED numbers, so they would have reported a 77 mm pipe that does not exist. They now report ACTUAL size, re-read from the model, and flag REFUSED or SNAPPED explicitly. The read-back must follow `Document.Regenerate()` — straight after `Set()` the parameter still echoes the request. See [`../knowledge/live-model/core.md`](../knowledge/live-model/core.md). Proved: asked 77 mm on the 'Default' conduit type and got 77 mm — conduit accepted it where pipe snapped, which is exactly why this has to be read back per element rather than assumed |
| [`create-wall.cs`](creators/create-wall.cs) | Create one straight basic Wall between two mm plan points (type, level, unconnected height, structural flag) — ✓ verified 2026-08-07: created a wall and checked it against the model rather than the message — length 4000 mm, height 3000 mm, `LevelId` 311, type resolved to the first basic WallType, wall count 8 -> 9, rolled back. Known shape left as-is: the report line runs AFTER `t.Commit()` and dereferences `wall.Id` with no null check, so if `Wall.Create` ever returned null the NRE would land in the catch, the "cannot roll back a committed transaction" error would be swallowed, and it would print FAILED about a wall that exists. Not reproducible on demand — recorded rather than papered over |
| [`create-ceiling.cs`](creators/create-ceiling.cs) | CONFIRMED IMPOSSIBLE on Revit 2020 — `Ceiling.Create` only exists from Revit 2022 (UI-only: Architecture > Ceiling) — fragment reports this instead of a compile error; existing ceilings still workable (ray-trace recipe, filters) |
| [`create-revision-cloud.cs`](creators/create-revision-cloud.cs) | Draw a rectangular Revision Cloud in any view/sheet tied to an existing Revision (rectangle in the view's own plane, so the same mm numbers work in plan/section/sheet) — completes the Revision lifecycle's annotation half — ✓ verified 2026-08-07: cloud created in the active view for Revision seq 1, and **read back** `RevisionId` 49030 (matching the target revision), `OwnerViewId` 312, and a bounding box of exactly 3000 x 2000 mm — so the view-plane maths (origin + right*x + up*y) puts it where asked. Both guards fired: an unknown SequenceNumber and an inverted rectangle |
| [`create-hvac-zone.cs`](creators/create-hvac-zone.cs) | Create an HVAC Zone on a Level and add existing Spaces to it — the grouping layer above `create-space.cs`; one-zone-per-space + phase-match gotchas in header — ✓ **verified live 2026-08-14**: zone created on Level 1 under phase "New Construction" (the doc's last phase, which is what `phaseName = null` resolves to), 1 space added, 0 rejected, and reading the zone back confirmed it really holds that Space. Note the new Zone reports `Area = 0 m²` while its Space reports 43.2 — the same delayed-computation pattern a freshly created Space shows, not a fault in the fragment |
| [`create-mep-system-type.cs`](creators/create-mep-system-type.cs) | New duct/pipe SYSTEM TYPE by duplicating an existing one + name/abbreviation/colour — how a project gets separately-filterable systems (CHWS, CHWR, Supply Air - Zone 1); no create-from-nothing API exists, hence the required source; feeds `filter-by-system-type.cs` — ✓ verified 2026-08-07, all four guard paths plus the real run: unknown domain, source type not found (lists what IS available), empty newTypeName, and an existing name (reports the clashing Id). The real run duplicated 'Supply Air' into 'MEP_VerifySupply' and **read back** abbreviation 'AJSA' and colour RGB(0,120,200) off the new type — the abbreviation being the field the header says matters most in daily use. Mechanical system types 3 -> 4, rolled back |
| [`create-callout-view.cs`](creators/create-callout-view.cs) | Callout view inside a parent view over an mm MODEL-coordinate rectangle — the last common view type `create-view.cs` excluded; inherits the parent's type by default — ✓ verified 2026-08-07 and **fixed live**: it reported `scale 1:20` while the callout was actually 1:100. On Revit 2020 a fresh callout's `VIEW_SCALE` parameter is READ-ONLY (so is `VIEW_SCALE_PULLDOWN_METRIC`) because a callout inherits its parent's scale — so the old guard `if (!pScale.IsReadOnly)` skipped the write **entirely** and the summary still printed the requested scale from the input variable. A claim with no API call behind it. It now reports the scale read back off the view and says plainly when a requested scale could not be applied. Re-proved both ways. Callout geometry, parent and ViewType all confirmed; both guards (bad parent, inverted rectangle) fired |
| [`create-legend-view.cs`](creators/create-legend-view.cs) | New Legend by DUPLICATING an existing one — the only route the API offers (no `ViewLegend.Create` on any version, and placing NEW legend components has no API either); reports that plainly when the project has zero legends — ✓ graceful path verified 2026-07-26 (zero-legend project); duplicate path needs a project that has one |
| [`create-sheet-list.cs`](creators/create-sheet-list.cs) | Sheet List / drawing index schedule (`ViewSchedule.CreateSheetList`) — schedules SHEETS not model elements, so `create-schedule.cs` can't make one; unschedulable field names reported with the available list — ✓ verified 2026-07-26 (created with both columns + sort, then reverted via native-undo) |
| [`create-key-schedule.cs`](creators/create-key-schedule.cs) | KEY schedule for a category (`ViewSchedule.CreateKeySchedule`) — the preset-lookup table; ALSO adds the key parameter to every element of that category, a model-wide change (say so first); key ROWS stay a manual job — ✓ verified 2026-08-07: created a Rooms key schedule, and **read the result back rather than trusting the inputs** — name 'MEP_VerifyRoomKeys', key parameter 'MEP Finish Key', and the real field order `Key Name, Floor Finish, Wall Finish` (Key Name being Revit's own automatic first column). 2 of 3 requested fields were schedulable; the bogus third was reported under "Not schedulable for Rooms, skipped" rather than silently dropped. **This also proves the 2026-08-04 compile fix at runtime**: `schedule.KeyScheduleParameterName` is the Revit 2020 route (`ScheduleDefinition.SetKeyName` does not exist), and the key name really did take. Schedules 1 -> 2, rolled back |

### Recipes (bespoke multi-stage builds, not filter+action shaped)
| Recipe | Job | Source |
|---|---|---|
| [`recipes/trace-mep-circuits.cs`](recipes/trace-mep-circuits.cs) | Bulk-cluster a filtered pipe/duct system into physical circuits and find real endpoints — ✓ verified 2026-07-23 (UnionWith fix) | `../knowledge/live-model/mep-trace.md` § Tracing real MEP connectivity |
| [`recipes/set-space-airflow.cs`](recipes/set-space-airflow.cs) | Create/find each room's MEP Space, set Supply/Return Airflow, cascade to existing terminals — ✓ verified 2026-07-23 incl. the cascade branch | `../knowledge/live-model/hvac-terminals.md` § HVAC air terminal layout |
| [`recipes/place-terminals-checkerboard.cs`](recipes/place-terminals-checkerboard.cs) | Place a room's supply/return terminals in a near-square checkerboard grid — ✓ verified 2026-07-23 exact | `../knowledge/live-model/hvac-terminals.md` § HVAC air terminal layout |
| [`recipes/place-fcu.cs`](recipes/place-fcu.cs) | Place an FCU, reposition toward the door, rotate to face terminals — API surface reflection-verified 2026-07-23, NOT live-executed (needs a Room+FCU+terminal layout beyond this model's current fixtures) | `../knowledge/live-model/hvac-equipment-placement.md` § Placing equipment relative to a door |
| [`recipes/draw-main-duct-with-cap.cs`](recipes/draw-main-duct-with-cap.cs) | Draw a sized main duct from the FCU and cap every open end correctly — API surface reflection-verified 2026-07-23, NOT live-executed (same layout gap as place-fcu.cs) | `../knowledge/live-model/hvac-ducts.md` § Drawing a duct, § cap-end recipe |
| [`recipes/connect-terminal-branch.cs`](recipes/connect-terminal-branch.cs) | Riser + real elbow + takeoff tee connecting a terminal to the main duct — API surface reflection-verified 2026-07-23, NOT live-executed (same layout gap as place-fcu.cs); core pattern since live-proven via connect-equipment-to-air-terminals.cs | `../knowledge/live-model/hvac-ducts.md` § Branch duct from a terminal |
| [`recipes/connect-equipment-to-air-terminals.cs`](recipes/connect-equipment-to-air-terminals.cs) | Full system in one pass: equipment supply connector → main trunk → tap+branch+elbow+drop per terminal → extend main past last branch → end cap → verify — the user's connection method end-to-end — ✓ verified 2026-07-26, live (6 terminals, 0 failures, warning-free) | `../knowledge/live-model/hvac-ducts.md` § The user's connection method |
| [`recipes/verify-duct-connectivity.cs`](recipes/verify-duct-connectivity.cs) | Trace every terminal's full connector chain to its FCU — ✓ mechanism verified 2026-07-23 (0-FCU state correctly detected; positive path needs a real FCU) | `../knowledge/live-model/hvac-ducts.md` (orphan-recovery trace) |
| [`recipes/slice-trunk-for-sizing.cs`](recipes/slice-trunk-for-sizing.cs) | HIGH RISK — slice a main trunk at each takeoff (grouped, checkerboard-aware), offset past the fitting body, for later per-segment sizing; joint held by a real Union fitting (2026-07-26 fix, technique live-proven that day on 4 trunks via inline build — bare ConnectTo joints get silently re-merged) — ✓ **re-verified live 2026-08-14** on a purpose-built 12 m / 3-piece / 4-takeoff fixture created entirely by API (`Duct.Create` + `NewTakeoffFitting`): 3 cuts at 2700/5700/7700 mm, 0 failed, all unions connected, and a full BFS trace confirmed **4 of 4 branches still reach the far trunk end**. **A silent bug was found and fixed in that run:** `onTrunkLine` used `Line.Distance()`, which on a bound LocationCurve measures to the SEGMENT, so a multi-piece trunk only ever found the piece whose Id was passed in — with 4 takeoffs it saw 1, `skipLastTakeoff` removed it, and the script reported "0 cut(s) made, 0 failed". Now uses perpendicular distance to the infinite line. The open `trunkDir` sign caution in the header still stands | `../knowledge/live-model/hvac-duct-sizing.md` § Slicing a main trunk into segments for duct sizing |
| [`recipes/split-duct-near-equipment.cs`](recipes/split-duct-near-equipment.cs) | Split a duct at a fixed gap from an equipment connector, joint held by a real Union fitting (2026-07-26 fix: bare ConnectTo joints can be silently re-merged) — NOT a standing default, only on explicit request — ✓ verified 2026-07-23 (BreakCurve Id-swap bug found+fixed, story in header), union fix not yet live-run | `../knowledge/live-model/hvac-ducts.md` § Splitting an existing duct into two segments at a given point |
| [`recipes/create-revisions-from-sheet-dates.cs`](recipes/create-revisions-from-sheet-dates.cs) | Scan sheet TextNotes for dates, create one project-level Revision per distinct date, oldest first | `../knowledge/live-model/revisions.md` |
| [`recipes/tag-elements-in-active-view.cs`](recipes/tag-elements-in-active-view.cs) | Tag every element of one category in the active view with a working L-shaped leader — direct live-model alternative to clicking Smart MEP Tags; simplified placement, not full clash-scoring | `../knowledge/live-model/tagging.md` § AJTools internal classes unreachable from scripts |
| [`recipes/ray-trace-to-ceiling.cs`](recipes/ray-trace-to-ceiling.cs) | Ray-cast straight up from each element to the nearest ceiling above it and snap the element's height to the hit point | the user's own idea (2026-07-14); positive case NOT yet live-verified — no Ceiling exists in this model yet |
| [`recipes/create-parametric-box-family-with-duct-connector.cs`](recipes/create-parametric-box-family-with-duct-connector.cs) | Family Editor authoring (not project-doc editing): set category, build a parametric box body extrusion + optional rectangular neck stub + duct connector, all resizable via Length/Width/Height/Neck Width/Neck Height/Neck Depth parameters — code-reviewed 2026-07-23, NOT live-executed (requires activating a Family Editor document, a visible workspace change not made without asking first) | `../knowledge/live-model/families.md` § Building a parametric family from scratch |
| [`recipes/create-equipment-family-from-datasheet.cs`](recipes/create-equipment-family-from-datasheet.cs) | Family Editor authoring: build a manufacturer equipment family from a product datasheet — parametric box cabinet (EQ-centred, back-at-origin option for wall-mounted kit), any number of round stubs carrying **pipe or electrical** connectors on the top/bottom face, and an optional toggleable clearance zone on its own subcategory with a transparent material — **every API call in it ran live 2026-08-10, but the composed file has NOT been run end-to-end** (generalized from a step-by-step build of the Condair EL 20-400V/3~ steam humidifier: 530×406×780, 5 connectors, 63 parameters, resize-tested at 700×500×1000 and reset clean; the INPUTS loops and the `backAtOrigin=false` branch are the untested parts). Encodes the connector-size-not-inherited fix, negative-end downward extrusions, and negative formula results driving an extrusion start. **Cannot save the .rfa** — `SaveAs` is blocked through the bridge, the user must do File → Save As | `../knowledge/live-model/families.md` § Fourth build (2026-08-10, Condair EL 20 steam humidifier from a manufacturer datasheet) |
| [`recipes/create-mep-line-standards.cs`](recipes/create-mep-line-standards.cs) | One-click MEP drafting line standard: line patterns (ISO 128 / ASME Y14.2 basis), MEP_-prefixed line styles capped at weight 3, object styles (matchline/callout/scope box/ref planes/grids), 2 filled region types, and a `MEP_Line_Styles_Legend` drafting view — idempotent, safe to re-run; deliberately NO system/service styles (the user's rule) | office standard — full rules in the script header |
| [`recipes/audit-flex-curves.cs`](recipes/audit-flex-curves.cs) | Audit every FLEX DUCT and FLEX PIPE — size, real spline length vs the direct line (the slack the drawing hides), bend points, whether both ends are genuinely connected, and which runs exceed the length YOUR project allows. Standalone, read-only, whole model or active view. Collects by category so it never names `FlexDuct`/`FlexPipe` and cannot hit the MEP sub-namespace compile trap — unproven, never run
| [`recipes/mep-grayout.cs`](recipes/mep-grayout.cs) | The whole "grayout for MEP" view standard in one pass — background flattened to grey `150` lines on `200` solid fill at weight 1, walls weight 2, floors lightest at `240`, doors `200` / windows `150` (a window sits inside the wall, so it cannot take the wall's fill colour as its line) both 100% transparent, services black at weight 3 and 80% transparent, insulation `80,80,80` weight 1 on `MEP_Hidden_Short_Dash`, mechanical equipment black with a `128` solid fill and no transparency, rebar + rebar couplers off. Per-view, never project-wide; reads every category back and reports the slots Revit discarded rather than claiming a clean apply — ✓ live-verified 2026-08-15 end-to-end on `PLAN AT EL. +100.950_HVAC Ground Floor Layout Copy 2` (1:50 HLR, no view template): 87 categories + 589 sub-categories written, rebar pair hidden, 63 categories lost a slot exactly as the header predicts, read-back confirms every value in the table | [`skills/ajtools-mep-grayout/SKILL.md`](../skills/ajtools-mep-grayout/SKILL.md) |
| [`recipes/create-mep-text-standards.cs`](recipes/create-mep-text-standards.cs) | One-click MEP text standard: 120 Arial text note types (6 sizes x 10 colours x box/no-box, `MEP_Anno_Arial_…` naming, Black = no suffix) plus a `MEP_Text_Styles_Legend` drafting view — idempotent, only missing types are created | office standard — full rules (exact RGB per colour) in the script header |
| [`recipes/model-health-audit.cs`](recipes/model-health-audit.cs) | One read-only whole-model health report — warnings by severity + top offenders, in-place families, embedded CAD imports, unenclosed Rooms/Spaces, views not on sheets, groups, unused templates/filters (dry-run counts); each section names its drill-down fragment — ✓ verified 2026-07-26 (found 3 unenclosed rooms, 16 views off sheets, 16 unused templates); its output wording had to be softened so the destructive-op guard stops refusing a read-only script | tool-gap backlog 2026-07-26 |
| [`recipes/fill-mm-document-register.cs`](recipes/fill-mm-document-register.cs) | Fill the MM_ document/handover register on ONE category in one pass — the fixed columns (CWA, MM_NP System Type, MM_Discipline Code, MM_Main Document Definition / Statement / Revision, Sub-Discipline) plus `MM_Main Drawing Number`, which is derived from **which sheet actually shows the element** (`FilteredElementCollector(doc, viewId)`, so view range, crop, hidden elements and view filters all count) rather than from its level. **The candidate sheets differ per category** — ducting items belong to the duct-layout sheets, equipment to the equipment-layout sheet; getting this wrong is silent, every element still receives a plausible sheet number. Writes a fixed value only into a BLANK, leaves any different existing value alone and reports it, refuses to write the drawing number at all if any element appears on two candidate sheets, pins the document by title, and reads every column back out of the model — ✓ verified live 2026-08-19 on 4355-BHVD-3D-60P00-BL006A across air terminals (45), duct accessories (88), flex ducts (6) and mechanical equipment (21) — 160 elements, 0 failures; it also caught two real things no fixed-value pass would: `MM_Main Drawing Statement`/`Revision` were **not bound to Flex Ducts** at all, and 4 exhaust louvres sit in the equipment category but are drawn on the duct sheets | Ajmal's register job 2026-08-19 |
| [`recipes/generate-room-coverage-layout.cs`](recipes/generate-room-coverage-layout.cs) | Device layout so a fixed coverage radius leaves no gap in a room, drawn — the sprinkler/detector/CCTV question. Two modes, and the choice matters more than the grid: **`inset`** (default, ISSUE THIS) keeps every centre inside the room and off the walls — square divides into equal cells, hexagonal staggers rows with nx/ny found by verified search; **`cover`** is the theoretical minimum-circle lattice + greedy set-cover. Verifies coverage AND buildability — **an unconstrained `cover` run put 6 of 21 centres outside the room and still reported "full coverage"** (2026-07-27), so the outside-centre count is always reported; greedy picks are also pruned (it over-reported 20 when 19 sufficed). Four optional code checks — max spacing, wall distance, MIN spacing, and max area per device — because full coverage is NOT compliance and any one of the four can fail alone (the area rule is the one a covering algorithm never looks at, and it sets a hard minimum device count). **For fire sprinklers route via [`skills/ajtools-fire-sprinkler-layout/SKILL.md`](../skills/ajtools-fire-sprinkler-layout/SKILL.md) and take the limits from [`knowledge/nfpa13-sprinkler-spacing.md`](../knowledge/nfpa13-sprinkler-spacing.md)** — NFPA has no coverage-radius concept, and 15 ft is 4,572 mm not 4,600 — ✓ verified 2026-07-27 (Room 4, 287.7 m², r=3000: `inset` square → 20 devices, 0 gaps, 0 outside, spacing 4,140×3,475 mm, wall 2,070 mm, both checks PASS; `inset` hexagonal → 19, 0 gaps, 0 outside, but both caps FAIL by ~20–30 mm; `cover` → 21 square / 19 hexagonal with centres outside the walls) | user's ask 2026-07-26, corrected 2026-07-27 |
| [`recipes/place-sleeves-at-wall-penetrations.cs`](recipes/place-sleeves-at-wall-penetrations.cs) | Find every duct/pipe crossing through straight host-model walls (centerline method) and — after the dry-run count is approved — place a sleeve family at each, rotated to the run, sized service + clearance; curved walls / non-straight runs skipped+counted — NOT yet live-verified, needs a sleeve family fixture (2026-07-26 round 2) | round-2 suggestions 2026-07-26 |
| [`recipes/build-test-fixtures.cs`](recipes/build-test-fixtures.cs) | **Builds the fixtures other fragments cannot be tested without** — ducts, insulation, an Assembly, a model Group, and (opt-in, one-way) worksharing + worksets. Clears the whole "needs a model that actually contains the fixture" open-items category for everything an API can build: unblocks the 3 insulation fragments, `filter-by-assembly`, `filter-by-group`/`action-ungroup-elements`, and the 5 worksharing ones. Deliberately does NOT re-attempt the settled impossibles (Ceilings 2022+, Scope Box, Phase create/rename, Design Option activation, workset delete) and names what still needs a real file (RVT link, CAD import, PDF driver, sleeve/flip families). Scratch-model only: refuses unless explicitly confirmed AND the model looks empty, and wraps everything in one TransactionGroup so a partial failure rolls back — NOT yet live-verified (2026-08-04, written without Revit; `AssemblyInstance.Create` and `NewGroup` are the two calls with no prior use in this library) | fixture-blocked open items, brain-log 2026-08-04 |

#### The fire sprinkler chain (2026-08-20)

Eight fragments that run in order, from "what is in this room" to "the heads are placed and re-checked".
Written together on Ajmal's ask to study sprinkler spacing and placement properly and build the tools for
it. **All eight are NOT yet live-verified** — written in a session with no Revit; every Revit call in them
is proven elsewhere in this library and each header names which. Run one element first, check the real
result, then trust it for the batch. All the rules and numbers live in
[`knowledge/fire-sprinkler/README.md`](../knowledge/fire-sprinkler/README.md), and the workflow that
routes between them is [`skills/ajtools-fire-sprinkler-layout/SKILL.md`](../skills/ajtools-fire-sprinkler-layout/SKILL.md).

| Fragment | Job | Knowledge |
|---|---|---|
| [`recipes/sprinkler-obstruction-survey.cs`](recipes/sprinkler-obstruction-survey.cs) | **Run this first.** Reads what is inside the room before any layout: is there a ceiling and at what level, what is the deck above, every beam with its soffit and how far it hangs, every column with the keep-clear circle it imposes, and every duct/tray/pipe/light wide enough to matter. Measures the **ceiling void** (ceiling top to slab soffit) and flags it against the 800 mm BS/EN trigger for sprinklers inside the void — while saying plainly that NFPA has no depth trigger and tests combustibility instead, so the two standards disagree on the same void. Detects the **structural bay module** from the parallel members and says what to feed the grid recipe — because in an exposed-structure room the bay, not the code's maximum spacing, sets the head count. Read-only — live-verified 2026-08-20 (Revit 2020, `Project1` Room 3: found the ceiling at 2,400 mm and correctly reported no beams/columns/services). **The ceiling/deck/void half is proven; the bay module, sloped-member and services branches are not** — the test model had none of them |  [`fire-sprinkler/obstructions.md`](../knowledge/fire-sprinkler/obstructions.md) |
| [`recipes/sprinkler-pipe-schedule-size.cs`](recipes/sprinkler-pipe-schedule-size.cs) | **Sizes sprinkler pipe by the PIPE SCHEDULE method** — walks the modelled network by clustering connector origins (never trusting `Connector.IsConnected`), counts how many heads each segment feeds, looks the size up, and reports required against modelled. **Checks the GATE first, and that is the point:** the schedule method is restricted to light/ordinary hazard and new systems of 465 m² or less (or larger with 50 psi proven at the highest head), and the 2025 edition removed the additions allowance — so on a normal Qatar job the honest output is usually "this needs hydraulic calculation", after which the sizes below are marked INDICATIVE ONLY for coordination. Detects a **looped/gridded** network, where a count-based schedule has no meaning at all, and flags the separate 8-per-branch rule. Tables are an editable input seeded UNCONFIRMED. Write-back is off by default and warns that Revit snaps a diameter to the Pipe Type's allowed list — NOT yet live-verified (2026-08-20, written with no Revit session; the connector-clustering walk is the shape live-proven in recipes/trace-mep-circuits.cs and the BFS is plain C#) | [`fire-sprinkler/pipe-sizing.md`](../knowledge/fire-sprinkler/pipe-sizing.md) |
| [`recipes/sprinkler-set-room-hazard.cs`](recipes/sprinkler-set-room-hazard.cs) | **Records the decided hazard class ON EACH ROOM** and reads it back, so the single most important sprinkler input stops being re-asked every session and a MIXED floor stops being flattened to one class. Reports which rooms have no class — the list that actually matters — and flags a class recorded with no standard named, since "Ordinary Hazard" is ambiguous between NFPA 13 and BS EN 12845. **It never decides the class**: the room-name matching is a suggestion engine for the report only and is never written, every suggestion says so, and write mode demands the SOURCE of the decision alongside it. Report mode is the safe default and also tells you whether the project parameters are even bound — NOT yet live-verified (2026-08-20, written with no Revit session; every Revit call in it is proven elsewhere in this library and the header names which) | [`fire-sprinkler/hazard-classification.md`](../knowledge/fire-sprinkler/hazard-classification.md) |
| [`recipes/sprinkler-floor-scope.cs`](recipes/sprinkler-floor-scope.cs) | **The first pass over a whole plan.** Walks every placed Room on a level and sorts each into needs-heads / special-treatment / **ASK**, with an area-rule head count per room and a floor total. **It never omits a room** — anything matching an omission rule goes in the ASK bucket with the reason, because a room silently dropped from a fire layout is the worst thing this Brain could produce. Classifies by room NAME and AREA only, and says so: every real omission rule turns on construction and combustible loading, which the model does not hold — NOT yet live-verified (2026-08-20, written with no Revit session; every Revit call in it is proven elsewhere in this library and the header names which) | [`fire-sprinkler/where-sprinklers-are-required.md`](../knowledge/fire-sprinkler/where-sprinklers-are-required.md) |
| [`recipes/sprinkler-layout-options.cs`](recipes/sprinkler-layout-options.cs) | **Several compliant layouts for one room, ranked — "give me another one".** Enumerates every nx × ny that satisfies every limit at once, collapses the ones that are the same decision, and prints a full check line each. Options differ on the four things that actually matter: head count, **branch direction** (a 5×4 and a 4×5 can cost the same and run their pipe at ninety degrees to each other), margin under the area cap, and bay alignment. **Every option printed passes every limit** — `rankBy` only sets the order, so nothing lower down is "less compliant". If it returns more than about eight, that is the finding: the code limits are not what decides this room, the structure or the ceiling grid is — NOT yet live-verified (2026-08-20, written with no Revit session; every Revit call in it is proven elsewhere in this library and the header names which) | [`fire-sprinkler/layout-method.md`](../knowledge/fire-sprinkler/layout-method.md) |
| [`recipes/sprinkler-nfpa-grid.cs`](recipes/sprinkler-nfpa-grid.cs) | How many heads and where, **derived from the code limits rather than from a coverage radius** — searches nx × ny for the smallest count satisfying area per head (`A_s = S × L`), max spacing, max/min wall distance and min head-to-head all at once, with every centre inside the real room boundary. Refuses to run without hazard class, construction type, area limit and spacing limit — none is derivable from the model. Prints a one-row-per-rule check table, the head centres, and a drafting radius derived as half the cell diagonal. Accepts a fixed bay module for one axis — live-verified 2026-08-20 (Revit 2020, four rooms, light hazard/NFPA 13; check table hand-checked against all four and exact). **The run's lesson: the 4,572 mm spacing cap was binding in every room, never the area rule** — quoting the area-rule floor as the head count would have under-counted all four |  [`nfpa13-sprinkler-spacing.md`](../knowledge/nfpa13-sprinkler-spacing.md) |
| [`recipes/sprinkler-obstruction-check.cs`](recipes/sprinkler-obstruction-check.cs) | Every head against every beam, column and service, applying the four rules in code order: ignorable (≤ 4 ft wide **and** ≥ 18 in below the deflector), too wide (needs heads underneath), three-times (**no 24 in cap for a column** — a 600 mm column needs 1,800 mm, the car-park trap), and the beam table. **The beam table is an editable input, seeded UNCONFIRMED** — the standard's own table could not be retrieved, so every run prints that warning until you type your edition's values in and set `beamTableConfirmed` — NOT yet live-verified (2026-08-20, written with no Revit session; every Revit call in it is proven elsewhere in this library and the header names which) | [`fire-sprinkler/obstructions.md`](../knowledge/fire-sprinkler/obstructions.md) |
| [`recipes/sprinkler-adjust-for-obstructions.cs`](recipes/sprinkler-adjust-for-obstructions.cs) | For each failing head, the **smallest move that clears it without breaking anything else** — spiral search, rejecting any candidate that breaches spacing, wall distance or the room boundary. Reports "no legal move" honestly instead of nudging a head into a different violation, and refuses to nudge at all for the wide-obstruction rule, which needs a head underneath. Dry-run by default; moving real elements needs an explicit go-ahead — NOT yet live-verified (2026-08-20, written with no Revit session; every Revit call in it is proven elsewhere in this library and the header names which) | [`fire-sprinkler/obstructions.md`](../knowledge/fire-sprinkler/obstructions.md) |
| [`recipes/sprinkler-deflector-height.cs`](recipes/sprinkler-deflector-height.cs) | **The "with ceiling how, without ceiling how" tool.** Rays up from each head, identifies what it hits, and from that decides the case: under a ceiling (25–305 mm below), exposed flat soffit (same window off the slab), or obstructed construction (25–152 mm below the **member**, and no more than the deck limit below the slab — **two limits, and it detects when a beam makes them impossible at once**, which is the signal for a head in each bay). Warns per category when a hidden category would make the rays lie — NOT yet live-verified (2026-08-20, written with no Revit session; every Revit call in it is proven elsewhere in this library and the header names which) | [`fire-sprinkler/deflector-and-ceiling-height.md`](../knowledge/fire-sprinkler/deflector-and-ceiling-height.md) |
| [`recipes/sprinkler-sidewall-layout.cs`](recipes/sprinkler-sidewall-layout.cs) | Sidewall heads along chosen wall segments, against the **sidewall table, not the pendent one** (light hazard 196 ft² / 14 ft, tighter for ordinary and needing a listed head). Lists the boundary segments so the wall is picked deliberately, flags heads that need a baffle, and makes the check a spacing-only tool never makes — **the throw across the room**: a room wider than the listed throw cannot be covered from one wall at any spacing — NOT yet live-verified (2026-08-20, written with no Revit session; every Revit call in it is proven elsewhere in this library and the header names which) | [`fire-sprinkler/sprinkler-types.md`](../knowledge/fire-sprinkler/sprinkler-types.md) |
| [`recipes/sprinkler-place-heads.cs`](recipes/sprinkler-place-heads.cs) | Places the real families at the computed centres (unhosted/level-based — the path that always works, unlike ceiling-hosted in an exposed room), then **reads the placed heads back out of the document** and reports whether the Ids still exist and landed where they were asked to. Written that way because this Brain has caught four separate silent-success bugs: a script's own report is not evidence. Refuses to place until `confirmPlacement` is set deliberately — live-verified 2026-08-20 (Revit 2020, 38 heads in one transaction, 0 failed, read-back and an independent audit both clean). **The read-back earned its keep on the pilot head: the placement point's Z is silently ignored on a OneLevelBased family** (asked 2,400, got 2,500) — write `Elevation from Level` afterwards and read it back |  [`fire-sprinkler/revit-modelling.md`](../knowledge/fire-sprinkler/revit-modelling.md) |
| [`recipes/sprinkler-compliance-audit.cs`](recipes/sprinkler-compliance-audit.cs) | "Check my sprinkler spacing" on heads that already exist, however they were arranged — area per head from a real **nearest-head partition** (not a grid formula, which does not apply to a nudged layout, and not room-area ÷ count, which flatters it), spacing from real nearest neighbours, wall distance from the real boundary. Names the heads that are over the area limit, and says explicitly what it did NOT check — live-verified 2026-08-20 (Revit 2020, 38 heads across four rooms, 0 failures, run from a separate bridge call and independently reproducing the grid recipe's figures). **The partition reports a LARGER worst-case area per head than the grid `A_s`** — 13.81 vs 13.15 m² on a perfectly regular grid, because edge cells are bigger. Both are correct; never swap them |  [`fire-sprinkler/layout-method.md`](../knowledge/fire-sprinkler/layout-method.md) |

### Commands (no element set)
The original 6 live-verified 2026-07-22; the 2 marked below are newer and not yet run.
| Command | Job |
|---|---|
| [`commands/native-undo.cs`](commands/native-undo.cs) | Revert the last transaction via Revit's own Undo — ✓ verified 2026-08-06 (a throwaway duct copy was committed, Undo posted, and a FRESH call confirmed it gone with the room/pipe/group/door/link/design-options all still present — `PostCommand` is async, so the check must be in a later call, never the same one) |
| [`commands/unhide-all-active-view.cs`](commands/unhide-all-active-view.cs) | Restore permanently hidden elements and clear Temporary Hide/Isolate in the active view — ✓ verified 2026-08-06 by changing the model: hid a duct (hidden 0→1), the fragment unhid it (1→0), then rolled back |
| [`commands/command-regenerate.cs`](commands/command-regenerate.cs) | Force `Document.Regenerate()` — for a composed script where a later step depends on geometry/properties an earlier step just changed — ✓ (standalone-transaction fix in header) — ✓ verified 2026-08-06 |
| [`commands/command-clear-selection.cs`](commands/command-clear-selection.cs) | Clear the active Revit selection — ✓ verified 2026-08-06 (3 ducts selected, then cleared to 0) |
| [`commands/command-activate-view.cs`](commands/command-activate-view.cs) | Switch the active view to a given View/ViewSheet — ✓ verified 2026-08-06 (switched to '2 - Mech' and back to '1 - Mech', confirmed by re-reading `Document.ActiveView`; bogus Id takes the not-found path) |
| [`commands/command-zoom-to-fit.cs`](commands/command-zoom-to-fit.cs) | Zoom the active view's open UI window to fit its current content — ✓ verified 2026-08-06 (active view had an open UI window; the no-window branch is unproven) |
| [`commands/command-compact-save.cs`](commands/command-compact-save.cs) | Save with Compact = true (Revit's "Compact File") + before/after file size — **BLOCKED for the UI document (proven 2026-08-10)**: `SaveAs`/`Save` throw *"not permitted when there is any open transaction phase started by API client"* because the bridge wraps every `run_csharp` in a TransactionGroup (`Document.IsModifiable` reads False, so it can't be used to detect this) — ask the user to save in Revit. **But NOT blocked in general (corrected 2026-08-11)**: a document created by `Application.NewFamilyDocument(...)` saves fine, which is what makes end-to-end family authoring possible — see `../knowledge/live-model/families.md` § Fourth build |
| [`commands/command-sync-with-central.cs`](commands/command-sync-with-central.cs) | Synchronize with Central + relinquish all (comment, save-local flags) — BLOCKED (model isn't workshared), graceful path only; sync writes the central, go-ahead first (2026-07-26 gap backlog) |

### Context (whole-document, read-only orientation — no element set, model never changes)
The original 9 live-verified 2026-07-22, zero bugs; newer ones marked individually.
| Fragment | Job |
|---|---|
| [`context/harvest-revit-api.cs`](context/harvest-revit-api.cs) | Dump the ENTIRE Revit API of the running Revit to disk by reflecting over the loaded `RevitAPI.dll` — every public type, property, method signature and enum value, one file per namespace, for the SEPARATE index in `api-index/`. Read-only, writes outside the model. Run once per Revit version; the API you get is the API you actually have, not a website's copy — unproven, never run
| [`context/context-session-start.cs`](context/context-session-start.cs) | **START HERE on every connection.** One call: Revit version + build, WHICH API GENERATION is live (64-bit ElementId? ForgeTypeId units? split Dimension classes?), document title/path/worksharing + central, project name/number/client, what unit the project actually DISPLAYS, model size, unloaded links, closed worksets, design options, phases, warning count, active view and current selection. Every section independently guarded, so one unreadable part cannot take down the report — unproven, never run
| [`context/context-active-view.cs`](context/context-active-view.cs) | Session snapshot — Revit version, active model (family/project, worksharing, open docs) + active view name/type/scale/level, screen Right/Up directions, open views, selection count. Standing follow-up to every successful ping (core.md rule) — ✓ verified 2026-08-06 (Revit 2020, reported version/doc/view/scale/level/directions/selection correctly) |
| [`context/context-project-units.cs`](context/context-project-units.cs) | Every unit spec valid for this document and its current display unit (mm/m, CFM/L/s, etc.) — ✓ verified 2026-08-06 (132 specs; the Revit 2020 `UnitType`/`DisplayUnitType` path in the header is the one that runs) |
| [`context/context-all-warnings.cs`](context/context-all-warnings.cs) | Every model warning — severity, description, failing element Ids; optional Error-only filter — ✓ verified 2026-08-06 (20 warnings; **some warnings return an EMPTY failing-element list** — system-level ones such as "flow direction mismatch" and "No Loss Defined" — so never assume every row names an element) |
| [`context/context-workset-info.cs`](context/context-workset-info.cs) | Worksharing on/off, and every user workset with open/closed state and owner — ✓ verified 2026-08-06, **both branches**: the not-workshared path, then the workshared path on a real local file (2 user worksets, owner and open-state correct) |
| [`context/context-model-categories.cs`](context/context-model-categories.cs) | Model categories, keyword-filterable (avoid an unfiltered full-model dump) — ✓ verified 2026-08-06 (keyword path, "Duct" → 9 categories) |
| [`context/context-used-families.cs`](context/context-used-families.cs) | Every loadable family in the model, excluding system and in-place families — ✓ verified 2026-08-06 (108 families) |
| [`context/context-design-options.cs`](context/context-design-options.cs) | Every Design Option — name, Id, Primary flag — orientation step before `filter-by-design-option.cs`/`action-set-design-option.cs` — ✓ verified 2026-08-06 (empty path only — the "options exist" branch is still unproven) |
| [`context/context-levels-and-grids.cs`](context/context-levels-and-grids.cs) | Every Level (name + elevation) and Grid (name) — feeds `create-dimension.cs`, `filter-by-grid.cs`, `filter-by-levels.cs` — ✓ verified 2026-08-06 (2 levels + 4 grids, mm conversion correct) |
| [`context/context-linked-models.cs`](context/context-linked-models.cs) | Every RVT link — loaded/unloaded status, pinned, workset — orientation step before `filter-by-links.cs`/`filter-by-linked-model-elements.cs`/`action-reload-links.cs` — ✓ verified 2026-08-06, **both branches**: the no-links path, then a real loaded link on a workshared model (status Loaded, workset resolved) |
| [`context/context-shared-coordinates.cs`](context/context-shared-coordinates.cs) | Project Base Point, Survey Point, active Project Location, True North rotation — reported in m + mm — the "is this model sitting/rotated right" orientation step — ✓ verified 2026-07-26 (note in header: a project can report more than one non-shared BasePoint) |

"Current selection" is already covered by [`filters/by-status/filter-by-current-selection.cs`](filters/by-status/filter-by-current-selection.cs) — not duplicated here.

### Examples (fully assembled)
| Example | Demonstrates |
|---|---|
| [`examples/color-isolate-select-by-size.cs`](examples/color-isolate-select-by-size.cs) | filter-by-category-and-numeric-param + 3 chained actions, the user's own worked scenario — ✓ **verified live 2026-08-14, run verbatim on its own default input (Height eq 500 mm)** against a 17-duct fixture holding 2 targets and 15 negatives at 400/300/250/200. All three actions confirmed in a SEPARATE bridge call, not from the script's own report: 2/2 coloured red with **0 wrongly coloured**, exactly those 2 visible under temporary isolate, exactly those 2 selected. One anomaly, recorded because it was seen and not explained: on the first run the selection read back as 0 in the next call while the script had reported "Selected 2". It did not reproduce — `SetElementIds` was then shown to work, to survive across bridge calls, and to survive an isolate-then-select in the same script. **If a selection ever looks empty after this example, re-read it before assuming the script failed** |
| [`examples/purge-unused-view-templates.cs`](examples/purge-unused-view-templates.cs) | filter-by-view-templates.cs (usage="unused") + action-delete-elements.cs — a destructive composition, run the filter alone first per the file's own MANDATORY note |
| [`examples/prelude-smoke-test.cs`](examples/prelude-smoke-test.cs) | Verifies every helper in `lib/prelude.cs` in ONE call — units, view targeting, collecting, parameters, level, size sort, and both the commit AND rollback paths of the transaction wrapper. Read-only in effect (its two transactions change nothing). Run this before trusting the prelude — ✓ verified 2026-08-07: **ran it, and every check PASSed**, which is what finally moved `lib/prelude.cs` off unproven. Its own value is now proven too: it caught nothing because there was nothing to catch, but the rollback assertion genuinely exercised a deliberate throw and confirmed the failure was reported rather than escaping to the bridge |

### Shared toolkit (`lib/`)
| Fragment | Job |
|---|---|
| [`lib/prelude.cs`](lib/prelude.cs) | The helpers 150+ fragments each re-implement — `InTransaction`/`InTransactionGroup` (start/commit/rollback/report), `ToFeet`/`ToMm`, `ResolveView`, `ParamOf`/`ParamText` (missing vs blank kept distinct), `LevelIdOf`, `CollectOf`, `SizeSortKey`. Paste FIRST, before the filter. **Additive — declares no name an existing fragment declares, in particular not `sb` or `elements`, so it composes with un-migrated fragments unchanged.** The real win is one place to be wrong: `DisplayUnitType` is named here twice instead of in 80 files, so a Revit 2021+ port is a two-line edit — ✓ **verified 2026-08-07 — every helper, all PASS**, via `examples/prelude-smoke-test.cs` against the real model. Highlights: 1000 mm -> 3.28084 ft -> 1000.00 mm exact round trip; `ParamText` returned `(blank)` for an empty Comments and `(no such parameter)` for a name that does not exist, which is the whole point of it; `LevelIdOf` resolved a Wall to Level 1; `SizeSortKey` sorted `unknown | 100ø | 250ø | 300x150 | 500x400 | 1200x600` with the ø intact; and `InTransaction` caught a deliberate exception, rolled back and REPORTED it instead of throwing to the bridge. It had sat unproven since 2026-08-04 — assembled from proven parts is not the same as proven, and this is the toolkit 150+ fragments are meant to share |


## The rules that apply to every script

## Always report the Element ID for specific elements

Any time output names/reports on **specific elements** (not a bare count) — a report table, a "here's
what I found/changed" list, a list of elements needing a decision — include each one's **Element ID** in
the output. It's the one identifier guaranteed unique per element in a model (see the "Element ID" entry
in [`../knowledge/glossary.md`](../knowledge/glossary.md)), so it's what lets the user re-select, verify,
or reference that exact element later (including via
[`filters/by-identity/filter-by-id-list.cs`](filters/by-identity/filter-by-id-list.cs)). The `action-report-*` fragments already
do this by default — keep that default on when writing a new one, and don't drop it just to shorten
output.

## Naming conventions (audited 2026-07-26 — keep new files inside these rules)

| Folder | Filename pattern | Example |
|---|---|---|
| `filters/` | `filter-by-<what>.cs` | `filter-by-size.cs` |
| `actions/**/` | `action-<verb>-<what>.cs` — always a VERB first | `action-set-view-range.cs` |
| `creators/` | `create-<what>.cs`, or the verb that's actually true | `create-duct.cs`, `load-family.cs` |
| `commands/` | `command-<what>.cs` | `command-activate-view.cs` |
| `context/` | `context-<what>.cs` | `context-project-units.cs` |
| `recipes/` | free — a plain-language description of the job | `place-sleeves-at-wall-penetrations.cs` |

Two rules matter more than the patterns:

- **Singular vs plural must never be the ONLY difference between two names.** Picking the wrong one of
  such a pair returns a confidently wrong answer instead of an error, which is the worst failure mode
  this library can have. `filter-by-level.cs`/`filter-by-levels.cs` and `filter-by-view.cs`/
  `filter-by-views.cs` were exactly that trap; they are now
  [`filter-by-elements-on-level.cs`](filters/by-location/filter-by-elements-on-level.cs) and
  [`filter-by-elements-in-view.cs`](filters/by-view-and-sheet/filter-by-elements-in-view.cs) — the name says what the
  fragment RETURNS. Keep doing that.
- **Read-only reporting actions use the `report` verb** so they sort together and read as safe at a
  glance: `action-report-*`. Nouns-first names (`action-material-takeoff.cs`) were renamed for this.

Three deliberate exceptions, kept on purpose — do not "fix" them:
[`load-family.cs`](creators/load-family.cs) (it loads from disk, it does not create — a `create-` prefix
would make the name lie), [`native-undo.cs`](commands/native-undo.cs) and
[`unhide-all-active-view.cs`](commands/unhide-all-active-view.cs) (both older, both more findable as they
are than with a `command-` prefix bolted on).

## Modular-by-default rule

A direct one-off snippet is fine for a quick live test, but if the idea is worth saving in
`scripts/`, convert it into reusable modules instead of saving the one-off shape.


## Explorer first, invoker second — for anything bulk or hard to reverse

For a request that's large in scope or not cheaply undone, **run the filter fragment alone first**
(paste just the filter, add your own `return sb.ToString();`, run it) to see the real count before
appending any action. Confirm that count matches what the user expects, *then* re-run the full composed
script with the action(s) attached. This is the same "confirm before bulk" rule already in
`START-HERE.md` and every HVAC skill — the filter/action split just makes the two steps literally separable instead of
having to mentally simulate the filter's result before running a monolithic script.

For a small, cheap, easily-undone request, just run the composed script directly — don't add ceremony
that doesn't earn its cost.

## Transaction safety — explicit rollback, never a silent throw

Every action fragment (and every `recipes/` script) wraps its `Transaction` in a try/catch that calls
`.RollBack()` and appends a clear reason to `sb` on failure, instead of letting an exception propagate
as a bare, uninformative error through the bridge. For a `recipes/` script with multiple dependent
transactions (draw a duct, then cap it), the whole sequence runs inside one `TransactionGroup` —
`group.Assimilate()` only on full success, `group.RollBack()` on any failure — so a mid-sequence error
can never leave a half-built result behind (e.g. a duct drawn but never capped). This came directly out
of a real incident: `draw-main-duct-with-cap.cs` once left an inconsistent model state after a partial
failure, which is exactly the failure mode a `TransactionGroup` prevents. Apply this same pattern
whenever a `recipes/` script is next touched, even if it hasn't been updated yet.

## Every number is a per-request input — never a default

Same rule as everywhere else in this project. Every fragment's `INPUTS` block (size in mm, color,
parameter name, room id, comparison operator) came from a specific request. Restate/confirm the current
one before running — the pre-filled values exist so there's one obvious place to edit, not because
they're safe to reuse blindly.


## Open every `run_csharp` call with a one-line `//` comment saying what it does

The first comment line is what the voice reads out loud before the script runs, so it is the only
warning the user gets about work that is about to change the model.

Ajmal, 2026-08-11, after asking for a colour change: *"its telling the voice only 'running the script'
— is that possible the shortly what is going to do with this scripting?"* He was right to ask. Native
tools announce themselves properly ("Counting air terminals", "Deleting 3 ducts") because the tool name
carries the meaning; a script does not, so without a comment every scripted job sounded identical.

```csharp
// Colour the supply ducts blue in this view      <- spoken as "Colour supply ducts blue in this view."
var ogs = new OverrideGraphicSettings();
```

Write it in **plain English, under ten words, as a person would say it** — not `// step 3` and not a
row of `====` (both are ignored). Only the first four lines are looked at, so it must be at the top.

`narrate-hook.mjs` now falls back to reading the API calls (`OverrideGraphicSettings` → "Changing
colour", `Document.Delete` → "Deleting elements", `Duct.Create` → "Drawing duct"), so a missing comment
is no longer silence — **but the fallback names the operation, not the intent.** "Changing colour" is
not "Colour the supply ducts blue". Write the comment.


## How to compose two or more fragments into one script

1. Pick the filter fragment that matches the request; open it and read its `INPUTS` block.
2. Pick one or more action fragments; read each one's `INPUTS` block too.
3. Paste the filter fragment's body first, then each action fragment's body in the order they should
   run, into one script. Every fragment shares the same two variable names — `elements` and `sb` — so
   they chain without any glue code. None of them end in `return`; you add exactly one
   `return sb.ToString();` as the very last line of the whole composed script.
   **Optionally paste [`lib/prelude.cs`](lib/prelude.cs) ahead of the filter** when a fragment you are
   writing or editing wants its helpers (`InTransaction`, `ToFeet`/`ToMm`, `ResolveView`, `ParamText`,
   `LevelIdOf`, `CollectOf`, `SizeSortKey`). It is optional today: no shipped fragment requires it, and
   it declares no name any fragment already declares, so adding it never breaks a composition. New
   fragments should prefer it — it is the one place `DisplayUnitType`, transaction rollback and the
   missing-vs-blank parameter rule are written down, instead of the 80/150/38 copies that exist now.
4. Fill in every `INPUTS` block with today's actual values — nothing pre-filled in these files is a
   default, per the rule below.
5. Run the composed script via `mcp__aj-tools-aj-ai__run_csharp`.

If the native MCP tool is not exposed in the current agent session, do not spend time re-reading
`mcp-server/index.js` or hand-writing a named-pipe wrapper. Use the checked-in fallback helper:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File ..\tools\invoke-bridge.ps1 -Ping
powershell -NoProfile -ExecutionPolicy Bypass -File ..\tools\invoke-bridge.ps1 -CodeFile <composed-script.cs>
```

This is a fallback only. If `mcp__aj-tools-aj-ai__run_csharp` is available, use the native MCP tool
directly.


## Before writing new AJ AI Bridge C#

Check `filters/` and `actions/` first — compose from what's there rather than writing a filter or an
action from scratch. Only write a new fragment if nothing existing covers the job; only write a
one-off, non-fragment script if it's genuinely not going to repeat (and even then, consider whether it's
actually a `recipe` in disguise).

## After running something new

If what you wrote (or composed) used a new *kind* of filter or action not covered here, save it as its
own fragment — or update the closest existing one — following the naming pattern
(`filter-by-<what>.cs`, `action-<verb>-<what>.cs`). If it was a true one-off, don't save it. Always
verify a fragment's result against the real model with a fresh read-back after running it (same
verify-don't-trust rule as `START-HERE.md`).


## After adding, updating, or retiring a fragment

Add one short dated line to [`../knowledge/brain-log.md`](../knowledge/brain-log.md) — same as any other
change to this Brain. If a fragment is retired because the job it did doesn't come up anymore, say so and
delete it rather than leaving a stale file that looks current.
